<?xml version="1.0" ?>
<Technology generated="auto" source="YAML frontmatter of markdown specs">
  <Feature source="20260510-auth-users-design.md" bd_id="aisw-hnl">
    <feature>auth-users-allowlist</feature>
    <status>stable</status>
    <date>2026-05-10</date>
    <stack>
      <Item>
        <python>3.11+</python>
      </Item>
      <Item>
        <tomllib>stdlib (read-only TOML parser)</tomllib>
      </Item>
      <Item>
        <pydantic>v2 (already in deps)</pydantic>
      </Item>
      <Item>
        <watchdog>6.0.0 (already in deps)</watchdog>
      </Item>
      <Item>
        <sqlalchemy>async (already in deps)</sqlalchemy>
      </Item>
    </stack>
  </Feature>
  <Feature source="20260510-classifier-stage0-design.md" bd_id="aisw-5">
    <feature>classifier-stage0-and-nl-time</feature>
    <status>stable</status>
    <date>2026-05-10</date>
    <stack>
      <Item>
        <python>3.11+</python>
      </Item>
      <Item>
        <pydantic>v2 (already in deps) — strict validation of CLI JSON output</pydantic>
      </Item>
      <Item>
        <dateparser>1.2.0 — rule-based NL time parsing, ru/en</dateparser>
      </Item>
      <Item>
        <structlog>already in deps</structlog>
      </Item>
      <Item>
        <asyncio>stdlib — subprocess via asyncio.create_subprocess_exec</asyncio>
      </Item>
      <Item>
        <sqlalchemy>async (already in deps) — audit.prompt_versions writes</sqlalchemy>
      </Item>
      <Item>
        <claude_cli>external binary, invoked via systemd-run --scope per call (chunk 16 wiring; chunk 5 stubs the wrapper)</claude_cli>
      </Item>
    </stack>
  </Feature>
  <Feature source="20260510-foundation-scaffold-design.md" bd_id="aisw-u99">
    <title>Foundation scaffold — Design</title>
    <feature>foundation-scaffold</feature>
    <date>2026-05-10</date>
    <status>draft</status>
    <stack>
      <python>3.11+</python>
      <package_manager>uv</package_manager>
      <lint>
        <Item>ruff (check + format)</Item>
        <Item>mypy --strict (src/ only)</Item>
        <Item>gitleaks</Item>
      </lint>
      <config>pydantic-settings v2</config>
      <logging>structlog (JSON renderer → stdout)</logging>
      <test>pytest + pytest-asyncio (anyio mode auto)</test>
    </stack>
    <deps_pinned>
      <runtime>
        <Item>pydantic==2.9.2</Item>
        <Item>pydantic-settings==2.6.1</Item>
        <Item>structlog==24.4.0</Item>
      </runtime>
      <deferred_runtime>
        <Item>aiogram==3.15.0</Item>
        <Item>APScheduler==3.11.0</Item>
        <Item>SQLAlchemy==2.0.36</Item>
        <Item>aiosqlite==0.20.0</Item>
        <Item>alembic==1.14.0</Item>
        <Item>dateparser==1.2.0</Item>
      </deferred_runtime>
      <optional_extras>
        <stt>
          <Item>faster-whisper==1.1.0</Item>
        </stt>
      </optional_extras>
      <dev>
        <Item>pytest==8.3.4</Item>
        <Item>pytest-asyncio==0.24.0</Item>
        <Item>ruff==0.8.4</Item>
        <Item>mypy==1.13.0</Item>
        <Item>pre-commit==4.0.1</Item>
      </dev>
    </deps_pinned>
    <adrs/>
  </Feature>
  <Feature source="20260510-wiki-lifecycle-design.md" bd_id="aisw-9s4">
    <feature>wiki-lifecycle</feature>
    <module_id>M-WIKI-LIFECYCLE</module_id>
    <status>stable</status>
    <date>2026-05-10</date>
    <discovery>docs/superpowers/specs/20260510-wiki-lifecycle-discovery.md</discovery>
  </Feature>
  <Feature source="20260510-wiki-runner-design.md" bd_id="aisw-x30">
    <feature>wiki-runner</feature>
    <module_id>M-WIKI-RUNNER</module_id>
    <status>stable</status>
    <date>2026-05-10</date>
    <discovery>docs/superpowers/specs/20260510-wiki-runner-discovery.md</discovery>
  </Feature>
  <Feature source="20260511-chunk10-tg-text-design.md" bd_id="aisw-187">
    <feature>M-TG-TEXT</feature>
    <status>stable</status>
    <date>2026-05-11</date>
    <chunk>10</chunk>
  </Feature>
  <Feature source="20260511-integration-e2e-design.md" bd_id="aisw-vb9">
    <feature>integration-e2e</feature>
    <module_id>M-INTEGRATION-E2E</module_id>
    <date>2026-05-11</date>
    <status>stable</status>
    <stack>
      <Item>pytest 8.3 + pytest-asyncio (auto mode) — already in pyproject</Item>
      <Item>aiosqlite + SQLAlchemy async — for tmp-path jobs/audit DBs</Item>
      <Item>structlog — observability, default config</Item>
      <Item>real ClaudeCliBackend → claude binary on PATH</Item>
    </stack>
    <contracts>
      <Item>tests/integration/conftest.py — fixtures (audit_sm, jobs_sm, fake_aiogram_bot, fake_runner, fake_output, fake_voice, fake_photo, real_classifier_adapter, inbox_root)</Item>
      <Item>tests/integration/test_e2e_pipeline.py — 4 scenarios</Item>
    </contracts>
  </Feature>
  <Feature source="20260511-runtime-wiring-design.md" bd_id="aisw-cq4">
    <feature>M-RUNTIME-WIRING</feature>
    <status>stable</status>
    <date>2026-05-11</date>
    <chunk>18</chunk>
    <approach>single-file-entrypoint</approach>
    <stack>
      <Item>aiogram 3.x (Dispatcher.start_polling)</Item>
      <Item>APScheduler AsyncIOScheduler</Item>
      <Item>alembic.config.Config + alembic.command.upgrade (programmatic)</Item>
      <Item>signal (SIGINT/SIGTERM via asyncio loop.add_signal_handler)</Item>
      <Item>structlog</Item>
    </stack>
    <references>
      <Item>docs/superpowers/specs/20260511-runtime-wiring-discovery.md</Item>
    </references>
  </Feature>
  <Feature source="20260511-tg-document-full-design.md" bd_id="aisw-wku">
    <feature>tg-document-full</feature>
    <module_id>M-TG-DOCUMENT-FULL</module_id>
    <chunk>22</chunk>
    <date>2026-05-11</date>
    <discovery_ref>docs/superpowers/specs/20260511-tg-document-full-discovery.md</discovery_ref>
    <status>approved</status>
    <technology_stack>
      <Item>
        <name>pypdf</name>
        <version>&gt;=4.0,&lt;5</version>
        <purpose>Pure-python PDF text extraction (no system deps, no Tesseract).</purpose>
        <license>BSD-3</license>
        <alternatives_rejected>
          <Item>pdfplumber — heavier, depends on pdfminer.six; overkill for first-page-text.</Item>
          <Item>pymupdf — AGPL/commercial split, system poppler dependency.</Item>
          <Item>pdf2image — requires poppler binary, raster output, out of D-022 scope.</Item>
        </alternatives_rejected>
      </Item>
      <Item>
        <name>pytest (existing)</name>
        <purpose>Unit tests for all branches.</purpose>
      </Item>
    </technology_stack>
    <contracts_touched>
      <Item>M-TG-PIPELINE (extend on_document, add helpers _route_document, _extract_pdf_text, _safe_log_filename)</Item>
      <Item>M-INBOX (no change — reuse IdempotencyService.check_content/record_dedup_choice with kind=&quot;file&quot;)</Item>
      <Item>M-OPS-PII (no change — reuse PIIRedactor.hash_token)</Item>
      <Item>M-TG-MEDIA (no change — reuse PhotoIngestor.handle for image/* branch)</Item>
    </contracts_touched>
    <data_model_changes>none</data_model_changes>
    <api_changes>
      <Item>DefaultPipeline.on_document signature unchanged (telegram_id, chat_id, update_id, doc_bytes, mime, filename).</Item>
      <Item>Internal helpers added as private DefaultPipeline methods.</Item>
    </api_changes>
    <new_strings_ru>
      <Item>ACK_DOC_UNSUPPORTED_RU = 'Этот тип файла пока не поддерживается.'</Item>
      <Item>ACK_DOC_PDF_NO_TEXT_RU = 'Не вижу текста в PDF. Попробуйте отправить страницу как фото.'</Item>
      <Item>ACK_DOC_TOO_LARGE_RU = 'Файл слишком большой (лимит 25 МБ).'</Item>
    </new_strings_ru>
    <log_anchors>
      <Item>tg.pipeline.document.received { telegram_id, mime, size, sha256_short, hashed_filename }</Item>
      <Item>tg.pipeline.document.dedup_hit { sha256_short, action='duplicate_doc' }</Item>
      <Item>tg.pipeline.document.rejected { reason: 'unsupported_mime'|'too_large'|'pdf_no_text'|'pdf_parse_error', mime }</Item>
      <Item>tg.pipeline.document.routed_text { source: 'pdf'|'text', chars }</Item>
      <Item>tg.pipeline.document.routed_image { ext, sha256_short }</Item>
    </log_anchors>
    <verification_strategy>
      <unit_tests>
        <Item>test_on_document_pdf_extracts_text_and_routes_through_text_pipeline</Item>
        <Item>test_on_document_pdf_with_no_extractable_text_rejects_with_hint</Item>
        <Item>test_on_document_pdf_parse_error_rejects_gracefully</Item>
        <Item>test_on_document_text_plain_routes_through_text_pipeline_with_utf8_bom_decode</Item>
        <Item>test_on_document_image_jpeg_routes_through_photo_ingestor</Item>
        <Item>test_on_document_image_heic_rejected_as_unsupported</Item>
        <Item>test_on_document_unsupported_mime_rejects_with_ru_message</Item>
        <Item>test_on_document_octet_stream_treated_as_unsupported</Item>
        <Item>test_on_document_l2_dedup_hit_short_circuits_before_routing</Item>
        <Item>test_on_document_oversized_doc_rejected</Item>
        <Item>test_on_document_log_uses_hashed_filename_never_raw</Item>
      </unit_tests>
    </verification_strategy>
  </Feature>
  <Feature source="20260511-tg-pipeline-classifier-design.md" bd_id="aisw-96y">
    <feature>tg-pipeline-classifier</feature>
    <chunk_id>20</chunk_id>
    <module_id>M-TG-PIPELINE-CLASSIFIER</module_id>
    <status>approved</status>
    <created_utc>2026-05-11</created_utc>
    <discovery_ref>docs/superpowers/specs/20260511-tg-pipeline-classifier-discovery.md</discovery_ref>
    <stack>
      <language>Python 3.11+</language>
      <frameworks_used>
        <Item>aiogram 3.x (existing — no version bump)</Item>
        <Item>pydantic v2 (existing)</Item>
        <Item>structlog (existing)</Item>
        <Item>sqlalchemy.async (existing)</Item>
      </frameworks_used>
      <new_libraries/>
      <decisions>
        <Item id="DEC-TPC-1">
          <title>Three thin Protocols in tg/pipeline.py (Classifier, WikiRunner, OutputDelivery)</title>
          <choice>Define narrow Protocol surfaces inside tg/pipeline.py; concrete adapters wrapping classify()/run_wiki_session()/deliver_output() live in __main__.py.</choice>
          <alternatives_considered>
            <Item>Direct injection of low-level callables — rejected: leaks disk paths and audit_session_maker into pipeline; explodes ctor arg count.</Item>
            <Item>Cross-module adapter classes in dedicated tg/adapters.py — rejected: premature factoring for 3 small adapters used only at composition root; deferred until 2nd reuse case.</Item>
          </alternatives_considered>
          <rationale>Mirrors existing pattern (TgSender, Spawner, HaikuSummarizer Protocols). DefaultPipeline stays test-friendly with trivial fakes. Adapters in __main__.py keep all I/O concerns at composition root, per GRACE rule on module boundaries.</rationale>
        </Item>
        <Item id="DEC-TPC-2">
          <title>WikiRunner Protocol returns aggregated text, not events list</title>
          <choice>`WikiRunner.run(...) -&gt; WikiRunOutcome(run_id: str, text: str, latency_ms: int)`. Adapter extracts assistant text from `WikiRunResult.events` by concatenating `assistant_chunk` payload content fields.</choice>
          <alternatives_considered>
            <Item>Pass raw WikiRunResult through — rejected: couples DefaultPipeline to streaming event schema.</Item>
            <Item>Add aggregate_text(events) helper to wiki.runner and reuse — accepted for adapter implementation but kept out of pipeline.py contract.</Item>
          </alternatives_considered>
          <rationale>Pipeline cares about text-out only; events are an implementation detail of Stage-1 runner.</rationale>
        </Item>
        <Item id="DEC-TPC-3">
          <title>L2 dedup uses existing IdempotencyService.check_content (no new Inbox API)</title>
          <choice>Pipeline injects IdempotencyService directly (already does for L1). For text it calls check_content(telegram_id, &quot;text&quot;, text); on collision short-circuits with ru-only &quot;уже видел такое&quot; reply and records the L2 hit via record_dedup_choice(... action=&quot;auto_skip&quot;).</choice>
          <alternatives_considered>
            <Item>Add stage_text / stage_voice helpers to inbox/staging.py — rejected: not needed; would create surface that has only one caller.</Item>
          </alternatives_considered>
          <rationale>breakdown.xml chunk-20 phrasing maps to existing audit.seen_files semantics. Anti-hallucination — names match real code.</rationale>
        </Item>
        <Item id="DEC-TPC-4">
          <title>Error handling at pipeline boundary — never propagate</title>
          <choice>try/except around each of (classify, runner.run, output.deliver). Each except branch logs (structlog .exception), emits one ru-only safe ack via sender, and returns. No raise.</choice>
          <alternatives_considered>
            <Item>Let aiogram's outer error middleware handle — rejected: middleware swallow + retry is a footgun for already-deduplicated updates.</Item>
          </alternatives_considered>
          <rationale>User must always get a reply; chat UX requires single-message terminal state.</rationale>
        </Item>
        <Item id="DEC-TPC-5">
          <title>Voice path = stage_media (existing) + STT (existing) + classifier+runner+deliver on transcript</title>
          <choice>VoiceHandler.handle remains unchanged. After transcript obtained, run check_content with kind=&quot;text&quot; on transcript.text; then classifier; then runner; then deliver. If transcript.text is empty → fallback ack ACK_TEXT_RU.</choice>
          <alternatives_considered>
            <Item>Dedup on raw audio bytes (kind='voice') — accepted as L2 layer-A but kept separate from this chunk to avoid double-dedup edge cases. Transcript-level dedup covers the user-perceived duplicate case (re-record of same words).</Item>
          </alternatives_considered>
          <rationale>Same Claude pipeline is reused; voice is just text after STT.</rationale>
        </Item>
        <Item id="DEC-TPC-6">
          <title>Adapters wire wiki_path resolution</title>
          <choice>WikiRunner adapter resolves wiki_path via telegram_id → owner profile (existing convention in this repo: `/var/lib/ai-steward-wiki/wikis/&lt;telegram_id&gt;/`). For MVP-launch (single dev user) we hard-resolve from Settings.wikis_root / str(telegram_id). Multi-domain routing per D-029 stays deferred.</choice>
          <alternatives_considered>
            <Item>Inject a WikiPathResolver — rejected: premature, no second consumer yet.</Item>
          </alternatives_considered>
          <rationale>Resolves only one path today; one-line constant function.</rationale>
        </Item>
      </decisions>
    </stack>
    <log_markers>
      <Item>tg.pipeline.classify.begin {correlation_id, telegram_id, chars}</Item>
      <Item>tg.pipeline.classify.done {correlation_id, telegram_id, intent, confidence, latency_ms}</Item>
      <Item>tg.pipeline.inbox.l2_dedup_hit {correlation_id, telegram_id, kind, sha8}</Item>
      <Item>tg.pipeline.runner.dispatched {correlation_id, telegram_id, run_id, intent}</Item>
      <Item>tg.pipeline.runner.completed {correlation_id, telegram_id, run_id, exit_code, chars, latency_ms}</Item>
      <Item>tg.pipeline.deliver.sent {correlation_id, telegram_id, run_id, n_messages, output_bytes}</Item>
      <Item>tg.pipeline.classify.error {correlation_id, telegram_id, error_class}</Item>
      <Item>tg.pipeline.runner.error {correlation_id, telegram_id, run_id, error_class}</Item>
    </log_markers>
    <modules_touched>
      <Item>src/ai_steward_wiki/tg/pipeline.py</Item>
      <Item>src/ai_steward_wiki/__main__.py</Item>
      <Item>src/ai_steward_wiki/wiki/runner.py</Item>
      <Item>tests/unit/test_pipeline_classifier_wiring.py</Item>
      <Item>tests/integration/test_pipeline_classifier_e2e.py</Item>
    </modules_touched>
  </Feature>
  <Feature source="20260511-tg-pipeline-streaming-design.md" bd_id="aisw-a3z">
    <id>20260511-tg-pipeline-streaming</id>
    <chunk>21</chunk>
    <module>M-TG-PIPELINE-STREAMING</module>
    <status>approved</status>
    <decisions>
      <Item id="DEC-TPS-1">
        <title>Streaming trigger</title>
        <choice>Wall-clock 5s race (not pre-estimate). Latch-based placeholder; runner-completes-first → fast path single deliver; 5s elapsed → send placeholder + start StreamEditor.</choice>
        <rationale>WikiRunner has no estimated_duration_s; pre-estimate is speculative. Race is deterministic, simple, equivalent UX.</rationale>
        <supersedes>DEC-L2 (interpretation only — DEC-L2 intent preserved)</supersedes>
      </Item>
      <Item id="DEC-TPS-2">
        <title>Runner streaming hook</title>
        <choice>Add on_event callback param to run_wiki_session. Additive, default None → existing behaviour. Callback awaited inside drain loop; exceptions logged and swallowed (runner not impacted).</choice>
        <rationale>Non-breaking, minimal API surface, keeps StreamEvent type internal to wiki package.</rationale>
      </Item>
      <Item id="DEC-TPS-3">
        <title>Streaming component placement</title>
        <choice>New class StreamingDelivery in tg/pipeline.py (alongside DefaultPipeline). Owns placeholder send, race timer, StreamEditor lifecycle.</choice>
        <rationale>Same module as composition orchestrator; reuses TgSender; not framework-thin enough to warrant separate file.</rationale>
      </Item>
      <Item id="DEC-TPS-4">
        <title>Final text source-of-truth</title>
        <choice>After runner.run returns (or raises), aggregate_text(events) is the canonical final text used by both StreamEditor.finalize and OutputDelivery.deliver. Events captured by a callback into a local list owned by StreamingDelivery.</choice>
        <rationale>Single source — no drift between TG visible state and audit log.</rationale>
      </Item>
      <Item id="DEC-TPS-5">
        <title>Back-compat</title>
        <choice>StreamingDelivery is an optional Protocol injection on DefaultPipeline (param `streaming`). If None → existing chunk-20 deliver path. All existing unit tests remain valid unchanged.</choice>
        <rationale>Zero-risk rollout; tests prove no regression.</rationale>
      </Item>
      <Item id="DEC-TPS-6">
        <title>Voice path scope</title>
        <choice>Voice on_voice does NOT use streaming in chunk 21 (transcripts are short, typical answer fits one message).</choice>
        <rationale>Lowest risk MVP; can add later by feeding voice through same StreamingDelivery.</rationale>
      </Item>
    </decisions>
    <log_markers>
      <Item>tg.pipeline.stream.begin</Item>
      <Item>tg.pipeline.stream.chunk</Item>
      <Item>tg.pipeline.stream.final</Item>
      <Item>tg.pipeline.stream.error</Item>
    </log_markers>
  </Feature>
  <Feature source="20260512-inbox-wiki-cron-bridge-design.md" bd_id="aisw-kcz">
    <feature>inbox-wiki-cron-bridge</feature>
    <epic>aisw-t2r</epic>
    <phase>Inbox-WIKI Phase-D.a</phase>
    <status>draft</status>
    <created>2026-05-12</created>
    <discovery>docs/superpowers/specs/20260512-inbox-wiki-cron-bridge-discovery.md</discovery>
    <supersedes_in_scope>digest_job → split out to aisw-19o (Phase-D.b)</supersedes_in_scope>
    <technology>
      <decisions>
        <Item id="T-1">
          <text>Integration shape A (Stage-0 fast-path, tech-spec §6): _run_text_pipeline branches on ClassifierResult.intent == Intent.REMINDER BEFORE the Stage-1a Router. The Stage-1a Router and prompts/inbox.md are NOT touched. (Q&amp;A D-OQ-1.)</text>
        </Item>
        <Item id="T-2">
          <text>Explicit confirm via the Phase-C machinery: ConfirmationService.request_explicit(draft, keyboard_factory=build_route_confirm_keyboard) with category='reminder'; the jobs.Job row is created only on the confirm callback. No implicit-ack. (Q&amp;A D-OQ-2.)</text>
        </Item>
        <Item id="T-3">
          <text>jobs.Job (storage/jobs/models.py, already exists, currently unused) is the SSoT domain record for a scheduled reminder. APScheduler's SQLAlchemyJobStore (same jobs.db) holds the trigger. create_reminder_job writes the Job row + commits, THEN scheduler.add_job(...) — so a crash in the (millisecond) gap leaves at worst a pending row without a trigger (the reminder silently does not fire); no reconciliation pass in the MVP. (Q&amp;A D-OQ-4 / R-1.)</text>
        </Item>
        <Item id="T-4">
          <text>Firing handler is a stable module-level callable ai_steward_wiki.scheduler.firing:fire_job(job_id: int) taking only a picklable int — required for SQLAlchemyJobStore persistence. It reads the aiogram bot-sender + jobs sessionmaker from a module-level registry set once at startup via firing.set_firing_context(...). (Q&amp;A D-OQ-3.) NOTE: this deliberately differs from scheduler/maintenance.py's retention jobs, which pass non-picklable session_makers as add_job args and get away with it because they are re-registered on every boot (replace_existing=True); a one-shot reminder cannot be re-registered, so it must persist correctly with picklable args only.</text>
        </Item>
        <Item id="T-5">
          <text>NL time: classifier.time_parse.parse_time gains a keyword arg prefer_future: bool = False threaded into dateparser settings as PREFER_DATES_FROM='future' when True. The reminder path calls it with prefer_future=True so a bare past wall-clock time («разбуди в 6» at 21:00) rolls forward to the next future occurrence (TZ-aware → DST-safe). User TZ from users.toml UserRecord.tz, falling back to a new Settings field default_user_tz: str = 'Europe/Moscow'. (Q&amp;A D-R-1.)</text>
        </Item>
        <Item id="T-6">
          <text>ReminderPayload.message = distilled_payload['reminder_text'] if it is a non-empty str, else the raw user text. prompts/classifier.md is NOT bumped — its distilled_payload is already documented as an opaque bag of extracted entities, so reading an optional reminder_text opportunistically needs no prompt change. (Q&amp;A D-OQ-1 / R-2.)</text>
        </Item>
        <Item id="T-7">
          <text>misfire_grace_time for the reminder DateTrigger = None (= 'run as soon as possible after a missed fire') so a reminder missed during downtime still arrives (late). An APScheduler EVENT_JOB_MISSED listener registered in __main__ logs scheduler.reminder.misfired. coalesce=True (job_defaults) means a backlog collapses to one fire.</text>
        </Item>
        <Item id="T-8">
          <text>No new third-party dependency. No new SQLite table (jobs.jobs exists). No new Alembic migration (jobs.Job columns suffice; APScheduler tables are owned by SQLAlchemyJobStore at runtime). One new Settings field (default_user_tz). prompts unchanged.</text>
        </Item>
      </decisions>
    </technology>
  </Feature>
  <Feature source="20260512-inbox-wiki-digest-interactive-design.md" bd_id="aisw-269">
    <feature>inbox-wiki-digest-interactive</feature>
    <phase>Inbox-WIKI Phase-D.b.2b</phase>
    <status>design</status>
    <date>2026-05-12</date>
    <discovery>docs/superpowers/specs/20260512-inbox-wiki-digest-interactive-discovery.md</discovery>
    <adr>docs/adr/ADR-025-digest-interactive-surface.md</adr>
    <covers>
      <Item>FR-1</Item>
      <Item>FR-2</Item>
      <Item>FR-3</Item>
      <Item>FR-4</Item>
    </covers>
    <stack_decisions>
      <Item id="TD-1">
        <text>aiogram Command filter for the first slash-command surface — a dedicated commands Router (or commands registered FIRST inside the existing m-tg-handlers-wiring router) so `/digest_now` and `/expand` match before the catch-all F.text message handler; ordinary text keeps falling through to MessagePipeline unchanged. No new dependency (aiogram.filters.Command is stdlib of aiogram 3.x).</text>
      </Item>
      <Item id="TD-2">
        <text>Reuse fire_digest_job(job_id) verbatim for /digest_now — no _run_digest core refactor, no new lock; the per-WIKI flock inside run_wiki_session already serialises a concurrent cron digest (queue-behind accepted).</text>
      </Item>
      <Item id="TD-3">
        <text>Generalise the DigestRunner Protocol with `section: str | None = None` (None ⇒ prompts/digest.md, today's behaviour byte-identical; key ⇒ prompts/digest_expand.md). _DigestRunnerAdapter in __main__ swaps overlay_prompt_path and the user_input accordingly. No new set_digest_context tuple element — the existing `runner` covers both modes.</text>
      </Item>
      <Item id="TD-4">
        <text>DigestPayload.wiki_scope: Literal['all'] → 'all' | list[str] (list min_length=1) in storage/jobs/payloads.py. No jobs.db Alembic migration ('all' stays valid; existing rows keep validating; Pydantic union discriminator unaffected — wiki_scope is not the discriminator).</text>
      </Item>
      <Item id="TD-5">
        <text>Heuristic WIKI-name extraction lives in the digest fast-path (tg/pipeline.py _handle_digest_intent / a small helper next to humanize_recurrence) — tokens of the NL turn ∩ the owner's *-WIKI/ dir-stems (case-insensitive, whole-token). No Haiku call. The owner's WIKI dir list comes from the same resolver the firing context already has (resolve_owner_wikis) or the wiki-lifecycle listing used elsewhere in pipeline.py — pick whichever is already imported there.</text>
      </Item>
    </stack_decisions>
  </Feature>
  <Feature source="20260512-inbox-wiki-digest-job-design.md" bd_id="aisw-oqq">
    <feature>inbox-wiki-digest-job</feature>
    <epic>aisw-t2r</epic>
    <parent>aisw-19o</parent>
    <status>stable</status>
    <date>2026-05-12</date>
    <follows>aisw-kcz</follows>
    <technology_stack>
      <Item>
        <python>3.11+</python>
      </Item>
      <Item>
        <apscheduler>AsyncIOScheduler + SQLAlchemyJobStore + CronTrigger</apscheduler>
      </Item>
      <Item>
        <pydantic>v2 (discriminated union; Recurrence model)</pydantic>
      </Item>
      <Item>
        <aiogram>3.x</aiogram>
      </Item>
      <Item>
        <claude_cli>2.1.139 (subscription auth, run_wiki_session)</claude_cli>
      </Item>
      <Item>
        <dateparser>fallback corpus for time-of-day inside recurrence phrasings</dateparser>
      </Item>
    </technology_stack>
    <approach>direct-fire-digest-job</approach>
  </Feature>
  <Feature source="20260512-inbox-wiki-digest-presentation-design.md" bd_id="aisw-w3k">
    <feature>inbox-wiki-digest-presentation</feature>
    <phase>Inbox-WIKI Phase-D.b.2a (presentation core)</phase>
    <status>design</status>
    <date>2026-05-12</date>
    <depends_on>
      <Item>aisw-oqq</Item>
    </depends_on>
    <discovery>docs/superpowers/specs/20260512-inbox-wiki-digest-presentation-discovery.md</discovery>
    <covers_fr>
      <Item>FR-1</Item>
      <Item>FR-2</Item>
      <Item>FR-3</Item>
      <Item>FR-4</Item>
      <Item>FR-10</Item>
    </covers_fr>
    <approach>Route fire_digest_job's delivery through the already-built tg.output.deliver_output(kind='digest'); grow set_digest_context by audit_session_maker; replace the one-line planner stub with a real jobs.db window query; rewrite prompts/digest.md to the D-024 HTML+TL;DR contract. No new module, no new dependency, no migration.</approach>
    <stack_decisions>
      <Item id="SD-1">
        <text>tg.output.deliver_output already implements D-024's &lt;b&gt;-header section split (ChainSplitter._BOUNDARY_PATTERNS puts &lt;b&gt; first), (i/M) footers, and the &gt;10000 send_document fallthrough. FR-2 needs NO new split code — only a digest-flavoured test + routing fire_digest_job through deliver_output. Rejected: a separate digest splitter (DRY violation).</text>
      </Item>
      <Item id="SD-2">
        <text>set_digest_context gains one required dep: audit_session_maker: async_sessionmaker[AsyncSession] (audit.db, for the run_outputs row written by deliver_output). _digest_ctx becomes a 6-tuple. No summarizer added — deliver_output's default LengthCapSummarizer is used (consistent with _OutputDeliveryAdapter; a real Haiku post-step summarizer is a project-wide future, not digest-specific — YAGNI).</text>
      </Item>
      <Item id="SD-3">
        <text>Planner context: a module-private async helper _build_planner_context(session, *, owner_telegram_id, window_hours, now_utc, tz) -&gt; str in scheduler/firing.py (it already holds the jobs session, parse_job_payload, and the Job model — KISS, no new module). Queries jobs.Job rows: status=='scheduled' AND scheduled_at_utc IS NOT NULL AND scheduled_at_utc &lt;= now_utc + window_hours, ordered by scheduled_at_utc; renders «- HH:MM — &lt;message&gt;» lines in the owner's tz, or «На ближайшие N ч ничего не запланировано.» when empty. tz comes from the digest job's own payload.recurrence.tz.</text>
      </Item>
      <Item id="SD-4">
        <text>prompts/digest.md rewritten to the D-024 contract: &lt;b&gt;📌 TL;DR&lt;/b&gt; as the FIRST section (3-5 lines), then only-if-content &lt;b&gt;📅 Сегодня&lt;/b&gt; / &lt;b&gt;💊 Лекарства&lt;/b&gt; / &lt;b&gt;📈 Трекеры&lt;/b&gt; / &lt;b&gt;📝 Обновления WIKI&lt;/b&gt;; HTML whitelist &lt;b&gt;&lt;i&gt;&lt;u&gt;&lt;s&gt;&lt;code&gt;&lt;pre&gt;&lt;a&gt;&lt;blockquote&gt;, escape &lt; &gt; &amp; in literal text, NO MarkdownV2; one short line per item, no tables; empty → exactly «🌿 Сегодня дел нет.»; consume the «Запланировано…» planner block from the user message. semver 0.0.1 → 0.1.0.</text>
      </Item>
      <Item id="SD-5">
        <text>fire_digest_job: after the runner returns text → body = text.strip() or _DIGEST_EMPTY_RU; run_id = f'digest-{uuid4().hex[:12]}'; runs_dir = primary_path / 'data' / 'runs'; await deliver_output(sender=, chat_id=, telegram_id=owner_id, wiki_id=primary_id, run_id=run_id, text=body, runs_dir=runs_dir, audit_session_maker=audit_maker, kind='digest', job_id=job_id) — wrapped in the existing try/except → _digest_strike. Remove _DIGEST_TG_LIMIT and its truncation (deliver_output owns sizing). The _DIGEST_NO_WIKI_RU branch stays a plain sender.send_message (it is a control message, not a Claude output → no audit row).</text>
      </Item>
      <Item id="SD-6">
        <text>__main__: set_digest_context(..., audit_session_maker=audit_maker); bump _DigestRunnerAdapter / wiring change-summary. The runner adapter is unchanged (it still returns raw text; deliver_output is called by fire_digest_job, not the adapter).</text>
      </Item>
    </stack_decisions>
    <log_anchors>
      <Item>scheduler.digest.delivered — now also carries n_messages, document_sent, run_id (from the DeliveryReceipt).</Item>
      <Item>scheduler.digest.planner_context — new DEBUG anchor: job_id, owner_telegram_id, n_planned (count of jobs in the window).</Item>
    </log_anchors>
    <adr>ADR-024 (digest presentation core) — records SD-1..SD-3 + the rejected alternatives. The Spec-WIKI D-024 'перенос в ADR' checkbox gets ticked.</adr>
    <out_of_scope_here>
      <Item>Actionable inline cards + callbacks (FR-5) → aisw-269.</Item>
      <Item>/expand, /digest_now (FR-6, FR-7) → aisw-269.</Item>
      <Item>Per-user section toggles + sessions.db 0002 (FR-8) → aisw-269.</Item>
      <Item>Named-subset WIKI selection (FR-9) → aisw-269.</Item>
      <Item>Real Haiku post-step summarizer wiring (project-wide, tracked elsewhere).</Item>
    </out_of_scope_here>
    <files_touched>
      <Item>src/ai_steward_wiki/scheduler/firing.py</Item>
      <Item>src/ai_steward_wiki/__main__.py</Item>
      <Item>prompts/digest.md</Item>
      <Item>docs/adr/ADR-024-digest-presentation.md</Item>
      <Item>docs/Spec-WIKI/decisions/D-024-digest-format.md</Item>
      <Item>docs/knowledge-graph.xml / verification-plan.xml / development-plan.xml</Item>
      <Item>tests/unit/scheduler/test_firing_digest.py</Item>
      <Item>tests/unit/tg/test_output.py</Item>
    </files_touched>
    <context7>Not required — no new library; SQLAlchemy async select on Job and uuid4 are already used in firing.py.</context7>
  </Feature>
  <Feature source="20260512-inbox-wiki-digest-section-toggles-design.md" bd_id="aisw-pv8">
    <feature>inbox-wiki-digest-section-toggles</feature>
    <phase>Inbox-WIKI Phase-D.b.2c</phase>
    <status>design</status>
    <date>2026-05-12</date>
    <discovery>docs/superpowers/specs/20260512-inbox-wiki-digest-section-toggles-discovery.md</discovery>
    <adr>docs/adr/ADR-026-digest-section-toggles.md</adr>
    <covers>
      <Item>FR-1</Item>
      <Item>FR-2</Item>
      <Item>FR-3</Item>
      <Item>FR-4</Item>
      <Item>FR-5</Item>
      <Item>FR-6</Item>
    </covers>
    <stack_decisions>
      <Item id="TD-1">
        <text>Storage shape — one row per owner with two boolean columns: user_digest_prefs(user_id INTEGER PK FK→users.user_id ON DELETE CASCADE, trackers_enabled BOOLEAN NOT NULL DEFAULT 1, wiki_enabled BOOLEAN NOT NULL DEFAULT 1, updated_at_utc DATETIME NOT NULL). NOT a generic (user_id, section_key, enabled) EAV table — the toggleable set is closed by ADR-025 (exactly `trackers`, `wiki`); TL;DR and `today` stay always-on. FK target = users.user_id (surrogate), mirroring inbox_hint_cache; resolved from telegram_id via storage.sessions.users.resolve_user_id. Absent row (or absent User) ⇒ both default True (opt-out feature — never opt-in).</text>
      </Item>
      <Item id="TD-2">
        <text>alembic/sessions/versions/0002_user_digest_prefs.py — first incremental sessions.db migration past 0001_sessions_baseline. revision='0002_user_digest_prefs', down_revision='0001_sessions_baseline'. CONSTRAINT: 0001's upgrade() is `Base.metadata.create_all` of LIVE metadata — so once the new UserDigestPrefs model is added to models.py, a from-empty `upgrade head` already creates user_digest_prefs in the baseline step; 0002 must therefore be IDEMPOTENT, not a bare `op.create_table` (which would raise 'table already exists' on fresh DBs). RESOLUTION: 0002.upgrade() = `Base.metadata.create_all(bind=op.get_bind())` (checkfirst=True by default → creates user_digest_prefs on an already-baselined existing DB, no-op on a fresh one); 0002.downgrade() = `op.drop_table('user_digest_prefs')`. render_as_batch=True (already in env.py) covers any future column ALTER. CONVENTION (recorded in ADR-026): a migration that only ADDS tables → add the ORM model to models.py + a new NNNN_* whose upgrade() calls `Base.metadata.create_all` (idempotent) and whose downgrade() explicitly drops the added table(s); a migration that ALTERs/drops existing columns → explicit op.* batch ops. Stepwise (0001→0002) and from-empty `upgrade head` must converge. jobs.db and audit.db untouched.</text>
      </Item>
      <Item id="TD-3">
        <text>Shared section-key vocabulary — TOGGLEABLE_DIGEST_SECTIONS: tuple[str,...] = ('trackers','wiki') (a subset of the existing tg/handlers.EXPAND_SECTION_KEYS = ('today','meds','trackers','wiki')) + SECTION_DISPLAY_NAME mapping key→ru label with emoji ('trackers'→'📈 Трекеры', 'wiki'→'📝 Обновления WIKI'). Defined once (next to EXPAND_SECTION_KEYS in tg/handlers.py) and reused by the command, the keyboard, the firing directive, and the migration/model column names. Test: set(TOGGLEABLE_DIGEST_SECTIONS) ⊆ set(EXPAND_SECTION_KEYS).</text>
      </Item>
      <Item id="TD-4">
        <text>Repo module storage/sessions/digest_prefs.py — frozen dataclass DigestPrefs(trackers_enabled: bool, wiki_enabled: bool) with property disabled_keys -&gt; tuple[str,...]; async get_digest_prefs(session_maker, telegram_id) -&gt; DigestPrefs (resolve user_id; no row / no User ⇒ DigestPrefs(True, True)); async set_digest_section(session_maker, telegram_id, *, section: str, enabled: bool) -&gt; DigestPrefs (upsert: create row with defaults if absent, set the one column, bump updated_at_utc; if telegram_id has no User row ⇒ no write, return DigestPrefs(True, True) — the command must not crash if invoked before onboarding).</text>
      </Item>
      <Item id="TD-5">
        <text>/digest_sections command — aiogram Command('digest_sections') handler registered in build_router alongside Command('digest_now')/Command('expand') (before the F.text catch-all). Replies with a ru header + InlineKeyboardMarkup: one button per toggleable section, text f'{SECTION_DISPLAY_NAME[k]}: {&quot;вкл ✅&quot; if on else &quot;выкл ⬜&quot;}', callback_data f'digestsec:{k}:{0 if on else 1}' (carries the TARGET state ⇒ idempotent on a stale message). New callback handler @router.callback_query(F.data.startswith('digestsec:')) → parse_digestsec_callback(data)-&gt;(section, target_enabled)|None → set_digest_section → edit_reply_markup with the rebuilt keyboard → cb.answer. parse_digestsec_callback: split ':' into exactly 3, section ∈ TOGGLEABLE_DIGEST_SECTIONS, flag ∈ {'0','1'}; else None. Independent from the confirm: prefix/parser. build_router gains access to the sessions session_maker (same channel firing.set_digest_context already uses for jobs/audit makers). Log anchors tg.command.digest_sections.shown / .toggled / .bad_callback. Ru-only strings, no i18n (D-032).</text>
      </Item>
      <Item id="TD-6">
        <text>Honour toggles in fire_digest_job — firing.set_digest_context gains a sessions_session_maker (or a bound get_digest_prefs); in fire_digest_job, after resolving the owner + WIKI set and before calling runner: prefs = await get_digest_prefs(sessions_session_maker, owner_telegram_id) (any failure ⇒ treated as DigestPrefs(True,True) — degrade to all-on, never skip the digest); disabled = prefs.disabled_keys; if disabled: planner_context = f'{planner_context}\n\nНе включай разделы: {&quot;, &quot;.join(SECTION_DISPLAY_NAME[k] for k in disabled)}.' and log scheduler.digest.sections_filtered{job_id, owner_telegram_id, disabled}; else planner_context unchanged (BYTE-IDENTICAL to today). runner(..., section=None) as today; the DigestRunner Protocol and _DigestRunnerAdapter are NOT changed (the directive rides inside the existing planner_context string). /expand path (run_section_expand) is NOT changed — an explicit /expand &lt;section&gt; overrides the toggle (the toggle governs only the unattended cron digest).</text>
      </Item>
      <Item id="TD-7">
        <text>prompts/digest.md — semver 0.1.0 → 0.1.1: add one sentence after the «Запланировано…» paragraph: «Если в сообщении есть строка вида „Не включай разделы: …&quot; — полностью пропусти перечисленные секции, даже если по ним есть содержимое.» prompts/digest_expand.md untouched.</text>
      </Item>
      <Item id="TD-8">
        <text>No new third-party dependency. Exactly one new SQLite table and one new Alembic migration, both sessions.db only. DigestPayload / storage/jobs/* untouched (no jobs.db migration). tg/pipeline.py untouched. All datetime UTC. mypy --strict for src/. structlog with ts/event/correlation_id/owner_telegram_id/job_id.</text>
      </Item>
    </stack_decisions>
  </Feature>
  <Feature source="20260512-inbox-wiki-hint-fastpath-phase-e-b-design.md" bd_id="aisw-5sd">
    <feature>inbox-wiki-hint-fastpath</feature>
    <epic>aisw-t2r</epic>
    <phase>Inbox-WIKI Phase-E.b</phase>
    <status>draft</status>
    <created>2026-05-12</created>
    <discovery>docs/superpowers/specs/20260512-inbox-wiki-hint-fastpath-phase-e-b-discovery.md</discovery>
    <decision>OQ-A → Option A (deterministic keyword/token-overlap, single-unambiguous-match threshold + margin). Confirmed at Brainstorming gate 2026-05-12.</decision>
    <technology>
      <decisions>
        <Item id="T-1">
          <text>New pure module inbox/hint_match.py: score_catalog(text: str, catalog: Mapping[str, str]) -&gt; HintMatch where HintMatch is a small frozen dataclass {ranked: list[tuple[str, float]] (stem, score, desc), top_stem: str | None, top_score: float, margin: float}. Scoring = normalised token-overlap: tokens(text) and tokens(hint) are lowercased word-runs (regex r'\w+' over the casefolded string, drop tokens shorter than MIN_TOKEN_LEN=3 and a tiny ru/en stop-word set); score(stem) = |tokens(text) ∩ tokens(hint_stem)| / max(1, |tokens(hint_stem)|); the domain stem name itself (minus the '-WIKI' suffix, casefolded, split on non-word) is folded into that domain's token set so 'health'/'здоровье' in the message also counts. Pure, deterministic, no IO.</text>
        </Item>
        <Item id="T-2">
          <text>‘Confident’ predicate (in hint_match.py): is_confident(m: HintMatch) -&gt; bool := m.top_stem is not None AND m.top_score &gt;= HINT_FASTPATH_MIN_SCORE AND m.margin &gt;= HINT_FASTPATH_MIN_MARGIN, where margin = top_score − second_score (second_score = 0.0 if only one domain scored &gt; 0). Default constants live in inbox/hint_match.py as module-level Final[float]: MIN_SCORE = 0.34, MIN_MARGIN = 0.17 (tunable; chosen conservative per NFR-2 precision-over-recall — re-tuned offline from the tg.pipeline.hint_fastpath.* logs). NOT exposed via settings.py in MVP (D-032 / YAGNI — no env knob until logs justify one).</text>
        </Item>
        <Item id="T-3">
          <text>Catalog build: a factory in __main__ — make_hint_catalog_resolver(*, hint_repo: InboxHintCacheRepo, owner_wikis_resolver: OwnerWikisResolver, surrogate_id_of: Callable[[int], Awaitable[int | None]]) -&gt; Callable[[int], Awaitable[dict[str, str]]]. The returned async resolver: uid = await surrogate_id_of(telegram_id); if uid is None → return {}; for (stem, dir_path) in await owner_wikis_resolver(telegram_id): hint = await get_or_refresh_hint(hint_repo, uid, dir_path / 'CLAUDE.md'); if hint: catalog[stem] = hint; return catalog. Reuses the existing get_or_refresh_hint stat→cache-hit path verbatim (0 fs reads on a hot message). surrogate_id_of is the existing users-table telegram_id→user_id lookup that __main__ already builds (the same surrogate every other sessions.db row uses — D-042); if no such helper is exposed yet, add a one-liner read over the users table in storage/sessions next to the engine setup.</text>
        </Item>
        <Item id="T-4">
          <text>DefaultPipeline gains ONE new optional kwarg: hint_catalog_resolver: Callable[[int], Awaitable[Mapping[str, str]]] | None = None (stored as self._hint_catalog_resolver). None ⇒ the fast-path block is a no-op fall-through — keeps every existing pipeline unit test green without the new dep. No other constructor change; owner_wikis_resolver stays as-is (the catalog resolver wraps it internally — the pipeline does not re-enumerate WIKIs).</text>
        </Item>
        <Item id="T-5">
          <text>New block START_BLOCK_HINT_FASTPATH … END_BLOCK_HINT_FASTPATH in tg/pipeline.py _run_text_pipeline, placed AFTER END_BLOCK_REMINDER_FASTPATH and BEFORE START_BLOCK_ROUTABLE_BRANCH. Guard: `if result.intent in _ROUTABLE_INTENTS and self._router is not None and self._hint_catalog_resolver is not None and self._librarian is not None and self._output is not None:` (the librarian/output guard mirrors START_BLOCK_ROUTABLE_BRANCH's confirm-path guard — no point bypassing the router if we cannot then run the confirm+ingest). Inside: catalog = await self._hint_catalog_resolver(telegram_id); if not catalog → log tg.pipeline.hint_fastpath.miss(reason='empty_catalog') and fall through. Else m = score_catalog(result.distilled_payload or text, catalog); log tg.pipeline.hint_fastpath.catalog(n_domains=len(catalog)). If not is_confident(m) → log tg.pipeline.hint_fastpath.miss(reason='ambiguous' if m.top_stem else 'no_match', top_stem, top_score, margin) and fall through. Else: synthesise decision = RouterDecision(intent=RouterIntent.ROUTE, target_wiki=m.top_stem, notes=&lt;short ru recap&gt;, parsed_ok=True, …) — exact field set mirrored from what _RouterAdapter returns for a ROUTE; payload = route_action_to_payload(decision, user_text=text, source=source, media_paths=media_paths, correlation_id=correlation_id); confirm_draft = PendingConfirmDraft(telegram_id, chat_id, category='route_ingest', draft=payload, recap_text=build_route_recap(decision)); rec = await self._confirm.request_explicit(confirm_draft, keyboard_factory=build_route_confirm_keyboard); log tg.pipeline.hint_fastpath.hit(target_wiki=m.top_stem, score=m.top_score, margin=m.margin, pending_id=rec.pending_id); return. (Falls through ⇒ START_BLOCK_ROUTABLE_BRANCH runs verbatim.)</text>
        </Item>
        <Item id="T-6">
          <text>Constructing a stand-in RouterDecision: read RouterDecision's field set from inbox/router.py at execution time and fill the minimum for a ROUTE — intent, target_wiki, parsed_ok=True, notes=ru one-liner ('Похоже на запись для «{stem}». Подтвердите перенос.' — wording finalised against the existing build_route_recap output so the recap reads naturally), and any other required fields with their natural defaults (e.g. an empty/structured payload field if RouterDecision has one). If RouterDecision has a required field we cannot meaningfully synthesise (e.g. a raw-CLI-output blob) → that is a deviation: stop, re-evaluate (advisory gate). route_action_to_payload / on_confirm_callback / Stage-1b librarian consume it unchanged.</text>
        </Item>
        <Item id="T-7">
          <text>structlog anchors (tg/pipeline.py, all under correlation_id + telegram_id): tg.pipeline.hint_fastpath.catalog(n_domains: int); tg.pipeline.hint_fastpath.hit(target_wiki, score, margin, pending_id); tg.pipeline.hint_fastpath.miss(reason: 'empty_catalog'|'no_match'|'ambiguous', top_stem: str|None, top_score: float, margin: float); tg.pipeline.hint_fastpath.fallthrough(reason) — emitted whenever we leave the block without short-circuiting (covers the guard-not-satisfied case at block top and the miss cases, so 'every bypass attempt observable' per FR-6). hint_text content is NEVER logged (it can quote arbitrary user-authored CLAUDE.md prose) — only stems, counts, scores. PIIRedactor already wraps the logger; no new redact rule needed (scores/counts/stems are non-PII).</text>
        </Item>
        <Item id="T-8">
          <text>No new third-party dependency, no new SQLite table, no new Alembic migration, no new user-facing string beyond the synthesised RouterDecision.notes ru one-liner (which is internal recap text, reusing the existing route-confirm copy family — D-032 ru-only honoured), no prompt change, no /wiki_* surface change (D-041), no change to _RouterAdapter / prompts/inbox.md / the heavy Router path. ops/retention and the media track are untouched.</text>
        </Item>
        <Item id="T-9">
          <text>Tests — all with fakes, no live Telegram/Claude/APScheduler/fs-outside-tmp_path: (a) tests/unit/inbox/test_hint_match.py — score_catalog on a hand-built catalog: single clear winner above thresholds → is_confident True; two domains both above MIN_SCORE within MIN_MARGIN → is_confident False (ambiguous); winner below MIN_SCORE → False (no_match); empty catalog → top_stem None; stem-name folding ('здоровье'/'health' in text matches Health-WIKI even if absent from the hint body); token normalisation (case, short-token drop, stop-words). (b) tests/unit/tg/test_pipeline_hint_fastpath.py — DefaultPipeline built with a fake hint_catalog_resolver returning a curated catalog + a fake confirm + a stub router whose .route is asserted NOT called on a confident hit and asserted called on miss/ambiguous/empty/disabled; assert a route_ingest PendingConfirmDraft was requested with category='route_ingest' on hit; assert the four log anchors fire on their respective paths (caplog/structlog capture). (c) existing tests/unit/tg/test_pipeline*.py untouched (new kwarg defaults to None). Coverage stays ≥80%.</text>
        </Item>
        <Item id="T-10">
          <text>grace-refresh + grace-refresh --verify after the change: knowledge-graph.xml — new module inbox/hint_match.py (M-INBOX-HINT-MATCH or folded into M-INBOX per the existing granularity; score_catalog/is_confident/HintMatch exports), DefaultPipeline DEPENDS += inbox.hint_match.score_catalog/is_confident + inbox.hint_cache.get_or_refresh_hint (transitively via the resolver dep), __main__ exports make_hint_catalog_resolver. verification-plan.xml — new test modules + the tg.pipeline.hint_fastpath.* log markers. Also clears the Phase-D-era 'hint fast-path' deferred note now that the cache is wired as a catalog.</text>
        </Item>
      </decisions>
    </technology>
  </Feature>
  <Feature source="20260512-inbox-wiki-hint-fastpath-staging-design.md" bd_id="aisw-12t">
    <feature>inbox-wiki-per-user-media-staging</feature>
    <epic>aisw-t2r</epic>
    <phase>Inbox-WIKI Phase-E.a</phase>
    <status>draft</status>
    <created>2026-05-12</created>
    <discovery>docs/superpowers/specs/20260512-inbox-wiki-hint-fastpath-staging-discovery.md</discovery>
    <supersedes_in_scope>bit 1 (## Inbox hint router-bypass) → split out to Phase-E.b (own feature-workflow iteration); this iteration = bit 2 only (per-user media _staging, subsumes aisw-64c)</supersedes_in_scope>
    <technology>
      <decisions>
        <Item id="T-1">
          <text>New pure helper inbox_wiki_path(telegram_id: int, *, wiki_root: Path) -&gt; Path in inbox/materialize.py returns wiki_root / str(telegram_id) / INBOX_WIKI_DIRNAME. No IO — path arithmetic only; the dir tree is created lazily by stage_media's mkdir(parents=True). ensure_inbox_wiki (CLAUDE.md first-contact materialiser) is unchanged and still owned by the _RouterAdapter. (OQ-A/R-2.)</text>
        </Item>
        <Item id="T-2">
          <text>Per-user staging root is injected into DefaultPipeline as wiki_root: Path (one new constructor kwarg, optional; None ⇒ media handlers fall back to the legacy fixed inbox_root they were constructed with — keeps existing pipeline unit tests that build handlers with a tmp inbox_root green without a wiki_root). The pipeline computes inbox_root = inbox_wiki_path(telegram_id, wiki_root=self._wiki_root) per message and passes it to the handler. (OQ-A: shape (b).)</text>
        </Item>
        <Item id="T-3">
          <text>VoiceHandler / PhotoIngestor: inbox_root moves from a required __init__ kwarg to an optional .handle(..., inbox_root: Path | None = None) override. __init__ keeps inbox_root as the default (so a handler can still be constructed standalone with a fixed root — used by integration tests and as the pipeline-fallback path). At call time: effective_root = inbox_root if inbox_root is not None else self._inbox_root. Both must not be None ⇒ a handler with no constructor root requires a per-call root (assert / ValueError). (OQ-A.)</text>
        </Item>
        <Item id="T-4">
          <text>New wrapper sweep_all_user_staging(wiki_root: Path, *, now: datetime | None = None, ttl_s: int = DEFAULT_STAGING_TTL_S) -&gt; int in inbox/staging.py: for each child of wiki_root that is a directory AND has an &lt;child&gt;/Inbox-WIKI/ subdir, call sweep_staging(child / INBOX_WIKI_DIRNAME, now=now, ttl_s=ttl_s) and sum the removed counts. Skips _trash/, stray files, and dirs without Inbox-WIKI. sweep_staging itself (keyed on one inbox_root) is unchanged. Emits media.staging_sweep_all_done(wiki_root, removed, scanned_users).</text>
        </Item>
        <Item id="T-5">
          <text>scheduler/maintenance.py: register_media_staging_sweep_job's staging_root: Path kwarg is renamed to wiki_root: Path; its job callable becomes _run_all_user_media_sweep(wiki_root, ttl_s) wrapping asyncio.to_thread(sweep_all_user_staging, ...). MEDIA_STAGING_SWEEP_JOB_ID, the 04:30 UTC cron, and idempotency (replace_existing=True) are unchanged. register_all_retention_jobs's media_staging_root: Path | None kwarg is renamed wiki_root_for_media_sweep: Path | None and threaded through. (The old _run_media_sweep over a single dir is removed — no internal backwards-compat shim.)</text>
        </Item>
        <Item id="T-6">
          <text>settings.media_staging_root is REMOVED (it was an explicit MVP placeholder per DEC-C1-2; nothing has shipped to prod that reads it). settings.py drops the field + its 3-line comment; the settings.py CHANGE_SUMMARY notes the removal. (OQ-B: remove, confirmed at Discovery gate.)</text>
        </Item>
        <Item id="T-7">
          <text>src/ai_steward_wiki/__main__.py: VoiceHandler/PhotoIngestor are constructed WITHOUT inbox_root (or with None); DefaultPipeline gains wiki_root=settings.wiki_root; register_all_retention_jobs is called with wiki_root_for_media_sweep=settings.wiki_root; the runtime.scheduler.started / runtime.media_pipeline.wired log fields that referenced media_staging_root now reference wiki_root. _WikiRunnerAdapter.run's promote_path_to_raw(media_path, wiki_root=&lt;target wiki&gt;) is unchanged — only the *origin* of the staged file changed (DEC-C4-1).</text>
        </Item>
        <Item id="T-8">
          <text>No new third-party dependency, no new SQLite table, no new Alembic migration, no new user-facing string, no prompt change. ops/retention.purge_staging (a separate, currently-unwired single-dir helper) is left untouched — out of scope; flag for a later cleanup task if it stays dead.</text>
        </Item>
        <Item id="T-9">
          <text>Tests: tests/unit/inbox/test_materialize.py +inbox_wiki_path; tests/unit/inbox/test_staging.py +sweep_all_user_staging over a mixed tree (a numeric user dir with stale+fresh staged files, a _trash dir, a stray file, a user dir with no Inbox-WIKI); tests/unit/tg/test_voice.py + test_photo.py updated for the .handle(inbox_root=...) override + the no-root-anywhere error; tests/unit/scheduler/test_maintenance.py updated for register_media_staging_sweep_job(wiki_root=...) + a _run_all_user_media_sweep test; tests/unit/tg/test_pipeline*.py — wherever a DefaultPipeline is built with voice/photo, pass wiki_root=tmp_path (or rely on the None-fallback). Coverage stays ≥80%.</text>
        </Item>
        <Item id="T-10">
          <text>grace-refresh + grace-refresh --verify after the change: knowledge-graph.xml (moved/renamed exports: sweep_all_user_staging, inbox_wiki_path, the maintenance signature) + verification-plan.xml (new log markers media.staging_sweep_all_done; removed media_staging_root references). Also clears the Phase-D report's deferred #6 for the media markers.</text>
        </Item>
      </decisions>
    </technology>
  </Feature>
  <Feature source="20260512-inbox-wiki-route-confirm-design.md" bd_id="aisw-e45">
    <feature>inbox-wiki-route-confirm</feature>
    <epic>aisw-t2r</epic>
    <phase>Inbox-WIKI Phase-C</phase>
    <status>draft</status>
    <created>2026-05-12</created>
    <approach>Confirm-gate the Phase-B route+ingest: a ROUTE/CREATE_WIKI RouterDecision is persisted as a `route_ingest` pending_confirms row (10-min TTL, draft_json) and proposed via a 2-button inline keyboard; the move+ingest runs in on_confirm_callback on 'confirmed'. Reuses ConfirmationService, the existing confirm:&lt;id&gt;:&lt;action&gt; handler, the PendingConfirm model (no migration), and the Phase-B Librarian.ingest path.</approach>
    <technology>
      <decisions>
        <Item id="TD-1">
          <text>Reuse `ConfirmationService` (D-023) as-is plus one additive change: `request_explicit(draft, *, keyboard_factory: Callable[[int], object] = build_explicit_keyboard)`. Route confirms pass `keyboard_factory=build_route_confirm_keyboard`. The chunk-10 explicit-confirm path keeps the default 3-button keyboard. No new confirm service, no parallel persistence.</text>
        </Item>
        <Item id="TD-2">
          <text>Reuse the `pending_confirms` table and `PendingConfirm` ORM model unchanged — `category`, `draft_json`, `chat_id`, `recap_message_id` columns exist since chunk 10. Route confirms set `category='route_ingest'` and `draft_json=json.dumps(route_action_payload)`. NO Alembic migration.</text>
        </Item>
        <Item id="TD-3">
          <text>Route-action (de)serialisation lives in `inbox/route.py` (it already owns `RouterDecision`): `RouteAction` frozen dataclass (decision: RouterDecision, user_text: str, source: Literal['text','voice','document','photo'], media_paths: list[str], correlation_id: str), `route_action_to_payload(decision, *, user_text, source, media_paths, correlation_id) -&gt; dict[str, object]`, `route_action_from_payload(payload: dict[str, object]) -&gt; RouteAction`. RouterIntent ↔ str via `RouterIntent(value)`/`.value`; RouterDecision ↔ dict via `.model_dump()`/`RouterDecision(**d)`; Path ↔ POSIX str.</text>
        </Item>
        <Item id="TD-4">
          <text>Russian recap / ack strings + the `build_route_recap(decision) -&gt; str` helper live in `tg/pipeline.py` next to the existing `*_RU` constants. `build_route_confirm_keyboard(pending_id) -&gt; InlineKeyboardMarkup` (2 buttons: `confirm:&lt;id&gt;:confirm`, `confirm:&lt;id&gt;:cancel`) lives in `tg/confirm.py` next to `build_explicit_keyboard`.</text>
        </Item>
        <Item id="TD-5">
          <text>`handlers.py` is untouched — `parse_confirm_callback` already accepts `confirm`/`correct`/`cancel`, and `pipeline.on_confirm_callback(telegram_id, chat_id, pending_id, action)` already receives everything. The route-vs-other dispatch happens inside `DefaultPipeline.on_confirm_callback` by reading the pending row's `category`.</text>
        </Item>
        <Item id="TD-6">
          <text>On `confirm` the pipeline sends a short ru ack («📝 Записываю в вики…») via `TgSender.send_message` BEFORE the (≤vision/text-timeout) `Librarian.ingest` call so the user isn't left hanging; the aiogram callback itself is already `answer()`-ed in `handlers.py`. Keyboard removal on the recap message is NOT done in the MVP (no `edit_reply_markup` on `TgSender`; a repeat tap is harmless — `resolve` returns `None` → 'stale' reply).</text>
        </Item>
        <Item id="TD-7">
          <text>Phase-B's immediate `librarian.ingest()` in `_run_text_pipeline`'s routable branch is REPLACED (not gated behind a flag). No `route_confirm_enabled` toggle. No new `proposed_actions` field on `RouterDecision`.</text>
        </Item>
        <Item id="TD-8">
          <text>No new third-party dependency. TTL expiry continues to rely on the existing `ConfirmationService.expire_due` sweeper job — no new scheduler entry. mypy --strict / ruff / ruff-format / grace lint clean; coverage ≥80%.</text>
        </Item>
      </decisions>
      <stack_unchanged>
        <Item>aiogram 3.x (InlineKeyboardMarkup/InlineKeyboardButton), SQLAlchemy async (pending_confirms), pydantic v2 (RouterDecision), structlog — all already in use.</Item>
      </stack_unchanged>
    </technology>
  </Feature>
  <Feature source="20260512-inbox-wiki-route-ingest-design.md" bd_id="aisw-zd9">
    <feature>inbox-wiki-route-ingest</feature>
    <epic>aisw-t2r</epic>
    <phase>Inbox-WIKI Phase-B</phase>
    <status>done</status>
    <created>2026-05-12</created>
    <discovery_ref>docs/superpowers/specs/20260512-inbox-wiki-route-ingest-discovery.md</discovery_ref>
    <approach>After Phase-A's router.route(), for ROUTE/CREATE_WIKI intents: resolve-or-create the target &lt;Domain&gt;-WIKI (WikiLifecycleManager), write a fresh raw payload + promote any media binary into the target WIKI, run a Stage-1b librarian session there (run_wiki_session + prompts/wiki.md + prompts/domain-*.md overlay), reply with notes + the ingest summary. New pure module inbox/route.py + a runtime _LibrarianAdapter behind a narrow Librarian Protocol; the pipeline orchestrates the two-step.</approach>
    <technology>
      <stack>
        <Item>
          <name>pydantic</name>
          <version>&gt;=2 (in project)</version>
          <use>WikiName already; RouteTarget/IngestOutcome are plain frozen dataclasses (no new pydantic types needed).</use>
          <context7>not needed — no library API in question; WikiLifecycleManager/run_wiki_session/promote_path_to_raw are internal, already read.</context7>
        </Item>
      </stack>
      <no_new_dependencies>True</no_new_dependencies>
      <no_new_db_table>True</no_new_db_table>
      <new_modules>
        <Item>src/ai_steward_wiki/inbox/route.py</Item>
      </new_modules>
      <changed_modules>
        <Item>src/ai_steward_wiki/tg/pipeline.py</Item>
        <Item>src/ai_steward_wiki/__main__.py</Item>
      </changed_modules>
      <decisions_ref>
        <Item>Q&amp;A 2026-05-12 (6 decisions) — see discovery_ref §Resolved design questions</Item>
      </decisions_ref>
      <adr_candidates>
        <Item>ADR — Inbox-WIKI Phase-B route+ingest: auto-create on missing ROUTE target; move-raw-before-Stage-1b (forward-only retry); dedicated Librarian adapter. (decide in Step 8 — leaning yes; it sets the Stage-1b ingest pattern Phases C–D build on, and ADR-003 already set the Stage-1a pattern.)</Item>
      </adr_candidates>
    </technology>
  </Feature>
  <Feature source="20260512-inbox-wiki-router-design.md" bd_id="aisw-dsg">
    <feature>inbox-wiki-router</feature>
    <epic>aisw-t2r</epic>
    <phase>Inbox-WIKI Phase-A</phase>
    <status>done</status>
    <created>2026-05-12</created>
    <discovery_ref>docs/superpowers/specs/20260512-inbox-wiki-router-discovery.md</discovery_ref>
    <approach>Router-Claude run in Inbox-WIKI/ with a fenced-block reply parsed into RouterDecision; wired via a dedicated _RouterAdapter behind a narrow Router Protocol; replaces the flat Stage-1 run for routable intents.</approach>
    <technology>
      <stack>
        <Item>
          <name>pydantic</name>
          <version>&gt;=2 (already in project)</version>
          <use>RouterDecision model (frozen, extra=forbid), RouterIntent str-enum.</use>
          <context7>not re-verified — already used pervasively (ClassifierResult); no version change.</context7>
        </Item>
        <Item>
          <name>aiogram</name>
          <version>3.x (already in project)</version>
          <use>unchanged — pipeline composition only.</use>
          <context7>not needed — no aiogram API touched.</context7>
        </Item>
      </stack>
      <no_new_dependencies>True</no_new_dependencies>
      <prompt_change>
        <file>prompts/inbox.md</file>
        <bump>1.0.0 → 1.1.0</bump>
        <change>add a strict response-format section requiring a single fenced ```router\ntarget_wiki: ...\nintent: ...\nnotes: ...\n``` block; ru-only notes.</change>
      </prompt_change>
      <new_modules>
        <Item>src/ai_steward_wiki/inbox/router.py</Item>
      </new_modules>
      <changed_modules>
        <Item>src/ai_steward_wiki/tg/pipeline.py</Item>
        <Item>src/ai_steward_wiki/__main__.py</Item>
      </changed_modules>
      <decisions_ref>
        <Item>Q&amp;A 2026-05-12 (5 decisions) — see discovery_ref §Resolved design questions</Item>
      </decisions_ref>
      <adr_candidates>
        <Item>ADR — Inbox-WIKI router invocation: dedicated adapter + fenced-block contract (decide in Step 8 whether it warrants an ADR; leaning yes — it sets the Stage-1a routing pattern for Phases B–E).</Item>
      </adr_candidates>
    </technology>
  </Feature>
  <Feature source="20260512-streaming-double-reply-design.md" bd_id="aisw-x92">
    <feature>streaming-double-reply</feature>
    <status>approved</status>
    <date>2026-05-12</date>
    <approach>tg-send-flag</approach>
    <technology_decisions>
      <Item id="TD-1">
        <text>Add keyword-only `tg_send: bool = True` to deliver_output (src/ai_steward_wiki/tg/output.py). When False: skip the entire size-hybrid send branch (send_message / chain-split / summary+send_document); still run _persist_to_disk and _record_run_output; emit tg.output.delivered with tg_send=False, n_messages=0, document_sent=False. Default True keeps current behaviour byte-for-byte.</text>
      </Item>
      <Item id="TD-2">
        <text>Propagate tg_send through OutputDelivery Protocol (pipeline.py:187) → _OutputDeliveryAdapter.deliver (__main__.py:242). Keyword-only, default True. mypy --strict must stay green.</text>
      </Item>
      <Item id="TD-3">
        <text>DefaultStreamingDelivery.run_and_deliver slow-path (pipeline.py ~968): change `await output.deliver(chat_id=..., telegram_id=..., run_id=..., text=final_text)` to `await output.deliver(..., tg_send=False)`. Fast-path call (pipeline.py ~895) and DefaultPipeline non-stream call are unchanged.</text>
      </Item>
      <Item id="TD-4">
        <text>StreamEditor (stream_edit.py): add instance field `_last_sent_text: str | None = None`, set it to the exact string passed to edit_message_text after every successful edit in feed() and in the chain-split branch. In finalize(): compute final_text; if final_text == self._last_sent_text → set _finalized=True, log tg.stream.finalized with skipped=True, return WITHOUT calling edit_message_text. Otherwise edit as today (keeping the existing try/except → tg.stream.final_flush_failed warning as a real-error fallback).</text>
      </Item>
      <Item id="TD-5">
        <text>Reject Variant 2 (split deliver_output into persist_and_audit + send_reply_to_tg): cleaner SoC but touches Protocol shape, DeliveryReceipt split, both call sites and more tests — disproportionate for an MVP bugfix. Revisit only if more delivery modes appear.</text>
      </Item>
      <Item id="TD-6">
        <text>Test approach: (a) tests/unit/tg/test_output.py — new case: deliver_output(tg_send=False) → fake sender records 0 send_message calls, but output file exists on disk and a run_output row is recorded; tg.output.delivered logged with tg_send=False. (b) tests/unit/tg/test_stream_edit.py — feed one chunk that triggers a tick, then finalize() → no second edit call, _finalized True, tg.stream.finalized skipped=True. (c) streaming pipeline test — slow-path run → exactly one TG message reaches the chat (the placeholder edit), deliver invoked with tg_send=False.</text>
      </Item>
    </technology_decisions>
  </Feature>
  <Feature source="20260512-system-prompt-replace-design.md" bd_id="aisw-adj">
    <feature>system-prompt-replace</feature>
    <status>stable</status>
    <date>2026-05-12</date>
    <technology_stack>
      <Item>
        <python>3.11+</python>
      </Item>
      <Item>
        <claude_cli>2.1.139 (subscription auth)</claude_cli>
      </Item>
    </technology_stack>
    <approach>inline-system-prompt</approach>
  </Feature>
  <Feature source="20260512-wiki-runner-cli-fix-design.md" bd_id="aisw-d3i">
    <feature>wiki-runner-cli-fix</feature>
    <date>2026-05-12</date>
    <status>draft</status>
    <discovery>docs/superpowers/specs/20260512-wiki-runner-cli-fix-discovery.md</discovery>
    <module_ids>
      <Item>M-WIKI-RUNNER</Item>
      <Item>M-CLASSIFIER-STAGE0</Item>
      <Item>M-CLAUDE-CLI-COMMON</Item>
    </module_ids>
    <chosen_approach>shared-primitives-single-file</chosen_approach>
    <stack>
      <Item>
        <python>3.11+</python>
      </Item>
      <Item>
        <stdlib>shutil, pathlib, asyncio</stdlib>
      </Item>
      <Item>
        <test>pytest</test>
      </Item>
    </stack>
    <new_modules>
      <Item id="M-CLAUDE-CLI-COMMON">
        <path>src/ai_steward_wiki/claude_cli/common.py</path>
        <purpose>Pure-function primitives shared by Stage-0 (classifier) and Stage-1 (wiki) Claude CLI backends.</purpose>
        <exports>
          <Item>resolve_binary</Item>
          <Item>build_env</Item>
          <Item>neutral_cwd</Item>
          <Item>system_prompt_argv</Item>
          <Item>truncate_stderr</Item>
        </exports>
      </Item>
    </new_modules>
    <changed_modules>
      <Item id="M-CLASSIFIER-STAGE0">
        <file>src/ai_steward_wiki/classifier/backend.py</file>
        <change>Refactor to import shared primitives. No behaviour change. Deletes 5 inline duplicates.</change>
      </Item>
      <Item id="M-WIKI-RUNNER">
        <file>src/ai_steward_wiki/wiki/runner.py</file>
        <change>(a) replace --append-system-prompt @path with --system-prompt-file &lt;path&gt;; (b) cwd=neutral_cwd; (c) read+truncate stderr; (d) raise WikiRunnerError on rc!=0; (e) require claude_config_dir; (f) assemble_prompt also folds in per-WIKI CLAUDE.md if present.</change>
      </Item>
    </changed_modules>
    <verification>
      <Item id="V-1">
        <text>New unit test pins argv for Stage-1 — asserts --system-prompt-file path equals assembled prompt and --append-system-prompt is absent.</text>
      </Item>
      <Item id="V-2">
        <text>New unit test pins cwd for Stage-1 — equals claude_config_dir, not wiki_path.</text>
      </Item>
      <Item id="V-3">
        <text>New unit test — fake Spawner returns proc with exit_code=1 + stderr=b&quot;bad flag&quot;; run_wiki_session raises WikiRunnerError and logs wiki.run.error with stderr truncated.</text>
      </Item>
      <Item id="V-4">
        <text>Existing Stage-0 envelope unwrap test stays green after refactor (no behaviour change).</text>
      </Item>
      <Item id="V-5">
        <text>New unit test for assemble_prompt — if wiki_path/CLAUDE.md exists, its contents appear after the overlay in the assembled file.</text>
      </Item>
    </verification>
  </Feature>
  <Feature source="20260512-wiki-runner-verbose-design.md" bd_id="aisw-kpb">
    <feature>wiki-runner-verbose</feature>
    <status>approved</status>
    <date>2026-05-12</date>
    <approach>add-verbose-flag</approach>
    <technology_decisions>
      <Item id="TD-1">
        <text>Add the literal flag '--verbose' to the argv list in _build_argv, adjacent to '--output-format stream-json'. No new dependency, no API change.</text>
      </Item>
      <Item id="TD-2">
        <text>Test strategy: extend the existing happy-path argv assertions in tests/unit/wiki/test_runner.py with `assert '--verbose' in argv` and a structural invariant check `('stream-json' in argv) implies ('--verbose' in argv)`.</text>
      </Item>
      <Item id="TD-3">
        <text>Reject alternative 'switch to --output-format json': the wiki runner and run_and_deliver are built around streaming delivery (per Spec-WIKI D-decisions); changing it is out of scope for a bugfix.</text>
      </Item>
    </technology_decisions>
  </Feature>
  <Feature source="20260513-digest-actionable-cards-design.md" bd_id="aisw-163">
    <title>Inbox-WIKI digest: actionable inline cards (±2h) — design</title>
    <date>2026-05-13</date>
    <phase>design</phase>
    <status>draft</status>
    <discovery>20260513-digest-actionable-cards-discovery.md</discovery>
    <related_adrs>
      <Item>ADR-025-digest-interactive-surface.md</Item>
      <Item>ADR-026-digest-section-toggles.md</Item>
      <Item>ADR-006-inbox-wiki-reminder-cron-bridge.md</Item>
    </related_adrs>
    <stack>
      <Item>
        <python>3.11+</python>
      </Item>
      <Item>
        <aiogram>3.x — CallbackQuery handlers + InlineKeyboardBuilder</aiogram>
      </Item>
      <Item>
        <sqlalchemy>2.0 async — single-row CAS UPDATE for state mutation</sqlalchemy>
      </Item>
      <Item>
        <alembic>per-DB (jobs/0002, sessions/0002)</alembic>
      </Item>
      <Item>
        <apscheduler>AsyncIOScheduler — DateTrigger for snooze rescheduling (existing path)</apscheduler>
      </Item>
    </stack>
    <modules>
      <new>
        <Item>M-DIGEST-CARDS</Item>
        <Item>M-TG-CALLBACKS</Item>
      </new>
      <changed>
        <Item>M-STORAGE-JOBS</Item>
        <Item>M-SCHEDULER-FIRING</Item>
        <Item>M-STORAGE-SESSIONS</Item>
        <Item>M-TG-HANDLERS-WIRING</Item>
        <Item>M-RUNTIME-WIRING</Item>
      </changed>
    </modules>
    <choice>Option A — Job row column for user_state + snooze_count</choice>
  </Feature>
  <Feature source="20260513-domain-prune-rename-design.md" bd_id="aisw-bgu">
    <feature>domain-prune-rename</feature>
    <date>2026-05-13</date>
    <status>approved</status>
    <approach>mechanical-rename-with-guardrail-merge</approach>
    <references>
      <discovery>./20260513-domain-prune-rename-discovery.md</discovery>
    </references>
    <stack>
      <Item>git mv for filesystem renames (preserves history)</Item>
      <Item>ruff + mypy + grace lint as gates</Item>
      <Item>no new dependencies</Item>
    </stack>
  </Feature>
  <Feature source="20260513-l2-dedup-ttl-design.md" bd_id="aisw-5hy">
    <feature>l2-dedup-ttl</feature>
    <module_id>M-INGEST-IDEM</module_id>
    <status>stable</status>
    <date>2026-05-13</date>
    <stack>
      <Item>
        <library>sqlalchemy</library>
        <version>2.x async (current uv.lock)</version>
        <used_for>SeenFile model + composite PK + ON CONFLICT clause</used_for>
      </Item>
      <Item>
        <library>alembic</library>
        <version>1.x (per-DB env audit)</version>
        <used_for>schema migration (PK swap, no data backfill)</used_for>
      </Item>
      <Item>
        <library>pydantic-settings</library>
        <version>2.x (current uv.lock)</version>
        <used_for>2 new Settings fields (text/voice TTL, binary TTL)</used_for>
      </Item>
    </stack>
    <decisions>
      <Item>
        <D-local-1>SeenFile PK becomes composite (owner_telegram_id, content_sha256). Existing PK on content_sha256 alone is replaced; `owner_telegram_id` loses its standalone index (now leading column of PK).</D-local-1>
      </Item>
      <Item>
        <D-local-2>check_content WHERE clause adds `first_seen_at_utc &gt; now - ttl_for_kind`. Older rows are treated as not-seen → ON CONFLICT path triggers `DO UPDATE SET first_seen_at_utc=excluded.first_seen_at_utc` (upsert), keeping the row but resetting the window.</D-local-2>
      </Item>
      <Item>
        <D-local-3>TTL per kind: `text/voice → AISW_L2_TTL_TEXT_SECONDS=60`, `photo/file → AISW_L2_TTL_BINARY_SECONDS=2592000` (30d). Both in Settings, .env-overridable.</D-local-3>
      </Item>
      <Item>
        <D-local-4>Migration drops legacy seen_files data (DELETE all rows then alter PK). Justification: forensic-only state, 30d retention, no business loss; alternative (data-preserving PK rebuild on SQLite) requires temp-table copy and is overkill for audit data.</D-local-4>
      </Item>
      <Item>
        <D-local-5>`inbox.idempotency.l2_duplicate` log event gains `within_ttl: bool` field. `l2_new` unchanged.</D-local-5>
      </Item>
      <Item>
        <D-local-6>ACK_DEDUP_RU message stays the same — only fires now when truly within TTL, so the meaning matches.</D-local-6>
      </Item>
    </decisions>
  </Feature>
  <Feature source="20260513-logging-coverage-chunk1-design.md" bd_id="aisw-er6">
    <feature>logging-coverage-chunk1</feature>
    <module_id>M-FOUNDATION-LOGGING</module_id>
    <status>draft</status>
    <date>2026-05-13</date>
    <references_discovery>docs/superpowers/specs/20260513-logging-coverage-chunk1-discovery.md</references_discovery>
    <stack>
      <Item>structlog (existing; ≥ 24.x in pyproject)</Item>
      <Item>aiogram 3.15 (existing)</Item>
      <Item>python 3.11+ inspect.iscoroutinefunction, time.perf_counter_ns, contextlib</Item>
      <Item>structlog.contextvars (bound_contextvars, clear_contextvars, bind_contextvars)</Item>
      <Item>structlog.testing.capture_logs (existing tests pattern)</Item>
    </stack>
    <new_modules>
      <Item>src/ai_steward_wiki/logging_events.py</Item>
      <Item>src/ai_steward_wiki/tg/middleware_correlation.py</Item>
    </new_modules>
    <modified_modules>
      <Item>src/ai_steward_wiki/logging_setup.py (add traced)</Item>
      <Item>src/ai_steward_wiki/tg/bot.py (register middleware)</Item>
      <Item>tests/conftest.py (autouse fixture)</Item>
    </modified_modules>
    <applied_to_existing>
      <Item>src/ai_steward_wiki/tg/pipeline.py (1–2 public entrypoints)</Item>
      <Item>src/ai_steward_wiki/classifier/__init__.py or stage entrypoints</Item>
      <Item>src/ai_steward_wiki/inbox/staging.py (1 entrypoint)</Item>
      <Item>src/ai_steward_wiki/wiki/run.py or equivalent run entrypoint</Item>
    </applied_to_existing>
    <adr_needed>False</adr_needed>
  </Feature>
  <Feature source="20260513-logging-coverage-chunk2-design.md" bd_id="aisw-nrt">
    <feature>logging-coverage-chunk2</feature>
    <status>draft</status>
    <date>2026-05-13</date>
    <risk>medium</risk>
    <evidence>strong</evidence>
    <approach>extend-in-place</approach>
    <stack>
      <Item>structlog (existing)</Item>
      <Item>apscheduler.events (existing dependency, listener API)</Item>
      <Item>sqlalchemy.event (existing dependency, before/after_cursor_execute)</Item>
      <Item>asyncio (existing — perf_counter for spawn duration)</Item>
      <Item>hashlib (stdlib — sha256 for statement_sha8)</Item>
    </stack>
    <adrs/>
    <links>
      <Item>chunk-1 logging_setup.py</Item>
      <Item>logging_events.py SSoT</Item>
    </links>
  </Feature>
  <Feature source="20260513-migrate-ai-steward-userdata-design.md" bd_id="aisw-0a5">
    <feature>migrate-ai-steward-userdata</feature>
    <title>Design — Миграция user-data ai-steward → ai-steward-wiki</title>
    <date>2026-05-13</date>
    <status>approved</status>
    <discovery_ref>20260513-migrate-ai-steward-userdata-discovery.md</discovery_ref>
    <risk>high</risk>
    <risk_justification>см. Discovery</risk_justification>
    <evidence>strong</evidence>
    <evidence_justification>ETL-архитектура классическая (Extract-Transform-Load), все целевые API верифицированы в src/ai_steward_wiki/{auth,storage/jobs,wiki}/, Pydantic discriminated union для payload validation на boundary.</evidence_justification>
    <open_questions/>
    <approach>ETL-tool как новый модуль `src/ai_steward_wiki/migration/` + CLI entrypoint `python -m ai_steward_wiki.migration --dry-run | --execute`. SSH snapshot vpn-0 в /tmp/migration-snapshot/ через rsync (manual step), ETL читает оттуда, пишет в production paths vpn-gpu-1.</approach>
    <stack>
      <Item>
        <python>3.11+</python>
      </Item>
      <Item>
        <sqlalchemy>same as project (orm-async)</sqlalchemy>
      </Item>
      <Item>
        <pydantic>v2 (validation на boundary)</pydantic>
      </Item>
      <Item>
        <tomli_w>для users.toml write (новая dep — добавить)</tomli_w>
      </Item>
      <Item>
        <structlog>same as project (JSON logs)</structlog>
      </Item>
      <Item>
        <pytest>same as project</pytest>
      </Item>
    </stack>
    <new_deps>
      <Item>tomli_w==1.2.0 (TOML write; tomllib только для read)</Item>
    </new_deps>
    <modules>
      <Item id="M-MIGRATION-CLI">
        <file>src/ai_steward_wiki/migration/__main__.py</file>
        <purpose>CLI entrypoint — argparse, dispatches dry-run vs execute, orchestrates phases.</purpose>
        <depends>
          <Item>M-MIGRATION-CONFIG</Item>
          <Item>M-MIGRATION-EXTRACT</Item>
          <Item>M-MIGRATION-TRANSFORM</Item>
          <Item>M-MIGRATION-LOAD</Item>
          <Item>M-MIGRATION-REPORT</Item>
        </depends>
      </Item>
      <Item id="M-MIGRATION-CONFIG">
        <file>src/ai_steward_wiki/migration/config.py</file>
        <purpose>Hardcoded mapping table (telegram_id ↔ source paths ↔ target WIKIs ↔ templates) per Q3 decisions. Single SSoT.</purpose>
        <depends/>
      </Item>
      <Item id="M-MIGRATION-EXTRACT">
        <file>src/ai_steward_wiki/migration/extract.py</file>
        <purpose>Read snapshot dir, parse planner.json, walk file trees, classify files (CSV/JSON/PDF/JPG/MD/.py/.bak). Returns SourceUserData per telegram_id.</purpose>
        <depends>
          <Item>M-MIGRATION-CONFIG</Item>
        </depends>
      </Item>
      <Item id="M-MIGRATION-TRANSFORM">
        <file>src/ai_steward_wiki/migration/transform.py</file>
        <purpose>Pure functions — planner-item → Job + JobPayload (fan-out по remind_before, monthly→cron, category map), file classification → target subpath, frontmatter generation.</purpose>
        <depends>
          <Item>M-MIGRATION-CONFIG</Item>
          <Item>ai_steward_wiki.storage.jobs.payloads</Item>
          <Item>ai_steward_wiki.classifier.recurrence</Item>
        </depends>
      </Item>
      <Item id="M-MIGRATION-LOAD">
        <file>src/ai_steward_wiki/migration/load.py</file>
        <purpose>Write users.toml (tomli_w), bootstrap WIKIs via WikiLifecycleManager, INSERT Job rows via SQLAlchemy, copy files (shutil.copy2 preserving mtime), write legacy-planner-history.md.</purpose>
        <depends>
          <Item>M-MIGRATION-TRANSFORM</Item>
          <Item>ai_steward_wiki.auth.users_toml</Item>
          <Item>ai_steward_wiki.storage.jobs</Item>
          <Item>ai_steward_wiki.wiki.lifecycle</Item>
        </depends>
      </Item>
      <Item id="M-MIGRATION-REPORT">
        <file>src/ai_steward_wiki/migration/report.py</file>
        <purpose>Aggregate per-user/per-project counters, generate migration_report.md.</purpose>
        <depends/>
      </Item>
    </modules>
    <ssot_artifacts>
      <Item>.env.example — add AISW_PROFILES_DIR</Item>
      <Item>data/users.toml — created at runtime by ETL</Item>
      <Item>pyproject.toml — add tomli_w dep</Item>
    </ssot_artifacts>
    <verification>
      <Item>
        <Unit_tests_для_transform.py__12-15_кейсов key="Unit tests для transform.py (12-15 кейсов">fan-out, monthly cron, category, recipients-mismatch fail-fast, date filtering, edge cases)</Unit_tests_для_transform.py__12-15_кейсов>
      </Item>
      <Item>
        <Integration_test key="Integration test">дам fixture-snapshot, прогон --dry-run, проверка counters в report</Integration_test>
      </Item>
      <Item>
        <Integration_test key="Integration test">--execute на тестовом target (отдельный tmp dir + temp SQLite), верификация результата</Integration_test>
      </Item>
      <Item>Manual cutover dry-run на staging (smaller snapshot или один user)</Item>
    </verification>
  </Feature>
  <Feature source="20260513-reminder-fastpath-fix-design.md" bd_id="aisw-5q5">
    <title>Reminder fast-path broken end-to-end — Design</title>
    <status>approved</status>
    <risk>medium</risk>
    <evidence>strong</evidence>
    <date>2026-05-13</date>
    <references>
      <discovery>docs/superpowers/specs/20260513-reminder-fastpath-fix-discovery.md</discovery>
    </references>
    <modules_touched>
      <Item>M-TG-PIPELINE</Item>
      <Item>M-CLASSIFIER-STAGE0</Item>
      <Item>M-CLASSIFIER-TIME</Item>
    </modules_touched>
    <libraries_verified/>
    <open_questions/>
    <adr_candidates/>
  </Feature>
  <Feature source="20260513-scheduler-jobstore-split-design.md" bd_id="aisw-6mi">
    <feature>scheduler-jobstore-split</feature>
    <module_id>M-SCHEDULER</module_id>
    <status>stable</status>
    <date>2026-05-13</date>
    <stack>
      <Item>
        <library>apscheduler</library>
        <version>3.x (current uv.lock)</version>
        <used_for>AsyncIOScheduler + MemoryJobStore + SQLAlchemyJobStore</used_for>
      </Item>
    </stack>
    <decisions>
      <Item>
        <D-local-1>Two named jobstores — &quot;default&quot; = SQLAlchemyJobStore (jobs.db, user reminders), &quot;memory&quot; = MemoryJobStore (infra cron)</D-local-1>
      </Item>
      <Item>
        <D-local-2>All maintenance registrars pass jobstore=&quot;memory&quot; to add_job</D-local-2>
      </Item>
      <Item>
        <D-local-3>On boot, after build_scheduler and before register_all_retention_jobs, run a one-time cleanup that removes known maintenance job ids from the default (SQLAlchemy) jobstore</D-local-3>
      </Item>
      <Item>
        <D-local-4>Cleanup pattern — exact id allowlist (PURGE_PENDING_JOB_ID, MEDIA_STAGING_SWEEP_JOB_ID, DB_SNAPSHOT_JOB_ID, retention.* via prefix on remove_all_jobs filter), not blanket purge of default store</D-local-4>
      </Item>
    </decisions>
  </Feature>
  <Feature source="20260513-start-help-manual-design.md" bd_id="aisw-s5i">
    <feature>start-help-manual</feature>
    <status>design</status>
    <date>2026-05-13</date>
    <depends_on_discovery>20260513-start-help-manual-discovery.md</depends_on_discovery>
    <technology>
      <stack>
        <Item>
          <name>aiogram</name>
          <version>3.15</version>
          <use>Router commands, BotCommand, set_my_commands</use>
          <verified_in_codebase>True</verified_in_codebase>
        </Item>
        <Item>
          <name>structlog</name>
          <version>(existing)</version>
          <use>Per-handler event logs</use>
          <verified_in_codebase>True</verified_in_codebase>
        </Item>
      </stack>
      <modules_touched>
        <Item id="M-TG-HANDLERS">
          <path>src/ai_steward_wiki/tg/handlers.py</path>
          <change>add three new Command handlers; import shared template loader</change>
        </Item>
        <Item id="M-TG-BOT">
          <path>src/ai_steward_wiki/tg/bot.py</path>
          <change>add register_bot_commands(bot) helper</change>
        </Item>
        <Item id="M-TG-MIDDLEWARE-AUTH">
          <path>src/ai_steward_wiki/tg/middleware_auth.py</path>
          <change>generalise _START_COMMAND_PREFIX → _PUBLIC_COMMAND_PREFIXES frozenset</change>
        </Item>
        <Item id="M-TEMPLATES">
          <path>src/ai_steward_wiki/templates.py</path>
          <change>NEW — package-level slug-validated template loader</change>
        </Item>
        <Item id="M-AUTH-ONBOARDING">
          <path>src/ai_steward_wiki/auth/onboarding.py</path>
          <change>format_intro_message becomes thin adapter over M-TEMPLATES</change>
        </Item>
        <Item id="M-RUNTIME">
          <path>src/ai_steward_wiki/__main__.py</path>
          <change>call register_bot_commands(bot) after build_dispatcher</change>
        </Item>
      </modules_touched>
      <new_files>
        <Item>templates/start-known.ru.md</Item>
        <Item>templates/help.ru.md</Item>
        <Item>templates/manual.ru.md</Item>
        <Item>tests/unit/test_templates.py</Item>
        <Item>tests/unit/tg/test_start_help_manual_handlers.py</Item>
        <Item>tests/unit/tg/test_register_bot_commands.py</Item>
      </new_files>
      <api_verifications_needed>
        <Item>aiogram.types.BotCommand signature (Context7 — first contact in session)</Item>
        <Item>bot.set_my_commands kwargs (Context7)</Item>
      </api_verifications_needed>
    </technology>
  </Feature>
  <Feature source="20260514-cron-add-queue-walking-skeleton-design.md" bd_id="aisw-02v">
    <feature>cron-add-queue-walking-skeleton</feature>
    <date>2026-05-14</date>
    <status>design</status>
    <risk>medium</risk>
    <risk_justification>4 modules touched (M-TG-CRON-ADD new, M-SCHEDULER-CRON-USER-PRODUCER new, M-SCHEDULER-CONSUMER new, M-STORAGE-JOBS payload widened); 1 __main__ wiring change; new user-facing TG command /cron_add (public-API surface); no DB schema migration (CronUserPayload column is JSON, no rows exist with old shape); no auth/security change; additive only.</risk_justification>
    <evidence>strong</evidence>
    <evidence_sources>
      <Item>Existing pattern M-SCHEDULER-FIRING (src/ai_steward_wiki/scheduler/firing.py v0.6.0) — APScheduler add_job + picklable int callback + module-level _ctx registry; mirrored 1:1 for cron-user producer</Item>
      <Item>Existing payload union src/ai_steward_wiki/storage/jobs/payloads.py — CronUserPayload already in discriminated union (no migration), widened to typed Recurrence (mirrors DigestPayload)</Item>
      <Item>Existing Recurrence + parse_recurrence in src/ai_steward_wiki/classifier/recurrence.py v0.0.3 (aisw-r2k) — reused without contract change</Item>
      <Item>Existing Lane.CRON_WRITE = 2 in scheduler/queue.py — semantically correct for cron-fired user CLI (corrected from discovery's 'USER' note)</Item>
      <Item>Existing kill_with_sequence in scheduler/core.py (D-021) — reused for systemd-run subprocess timeout path</Item>
      <Item>Existing tg/output.py ChainSplitter — text chunking helper reused standalone (full D-025 deliver_output deferred to follow-up issue with run-output persistence)</Item>
      <Item>Context7 verified 2026-05-14: aiogram v3 Router/Command/CommandObject API unchanged (matches handlers.py v0.4.0); APScheduler 3.x AsyncIOScheduler.add_job(callback, trigger, args=[int], id=str, replace_existing=True) is the still-current shape for the version pinned in this repo</Item>
      <Item>tech-spec §3 D-011 (5-lane queue, 600s CLI timeout) + D-021 (SIGTERM grace SIGKILL) + D-023 confirm pattern + D-042 identity vocab</Item>
    </evidence_sources>
    <open_questions/>
    <stack>
      <language>Python 3.11+</language>
      <package_manager>uv (pyproject.toml + uv.lock)</package_manager>
      <runtime_libs>
        <Item>
          <name>aiogram</name>
          <version_pin>3.x (as currently pinned in pyproject)</version_pin>
          <role>TG handler /cron_add via Router(Command('cron_add')); CommandObject.args for input</role>
          <context7_verified>True</context7_verified>
        </Item>
        <Item>
          <name>APScheduler</name>
          <version_pin>3.x (AsyncIOScheduler + SQLAlchemyJobStore — current pinned shape)</version_pin>
          <role>Persistent CronTrigger jobs (jobs.db SQLAlchemyJobStore); add_job(fire_cron_user_job, CronTrigger(**recurrence.to_cron(), timezone=user_tz), args=[job_id], id=str(job_id), replace_existing=True)</role>
          <context7_verified>True</context7_verified>
          <note>Context7 default snippets surface APScheduler 4.x (AsyncScheduler/add_schedule). NOT used here — repo is locked to 3.x, and the existing firing.py pattern is the authoritative reference.</note>
        </Item>
        <Item>
          <name>Pydantic v2</name>
          <version_pin>current</version_pin>
          <role>CronUserPayload widening (typed Recurrence) + in-memory CronQueueMsg discriminated union</role>
          <context7_verified>False</context7_verified>
          <note>API unchanged vs. existing payloads.py — no version sensitivity</note>
        </Item>
        <Item>
          <name>structlog</name>
          <version_pin>current</version_pin>
          <role>BLOCK-anchored logs with correlation_id, owner_telegram_id, chat_id, job_id</role>
          <context7_verified>False</context7_verified>
        </Item>
        <Item>
          <name>asyncio</name>
          <version_pin>stdlib</version_pin>
          <role>Subprocess spawn (asyncio.create_subprocess_exec), wait_for(timeout=600), consumer drain task</role>
        </Item>
      </runtime_libs>
      <external_processes>
        <Item>
          <name>systemd-run</name>
          <mode>--scope --collect</mode>
          <slice>aisw-cli.slice (existing per D-021/D-035)</slice>
          <role>Per-job CGroup scope; cleanup-on-exit via --collect; SIGTERM-grace-SIGKILL via kill_with_sequence</role>
        </Item>
        <Item>
          <name>claude (Claude Code CLI)</name>
          <auth>subscription via CLAUDE_CONFIG_DIR=/var/lib/ai-steward-wiki/claude-code</auth>
          <argv_builder>claude_cli/common.py (resolve_binary + build_env + system_prompt_argv + neutral_cwd) — reused</argv_builder>
        </Item>
      </external_processes>
    </stack>
    <modules>
      <M-TG-CRON-ADD>
        <file>src/ai_steward_wiki/tg/cron_add.py</file>
        <role>RUNTIME</role>
        <purpose>/cron_add TG handler — parse NL recurrence + command, persist Job via producer, reply with parsed schedule + job_id.</purpose>
        <depends>
          <Item>aiogram (Router, Command, CommandObject, Message)</Item>
          <Item>structlog</Item>
          <Item>ai_steward_wiki.classifier.recurrence.parse_recurrence</Item>
          <Item>ai_steward_wiki.scheduler.cron_user.create_cron_user_job</Item>
          <Item>ai_steward_wiki.storage.sessions.users (user_tz resolution)</Item>
        </depends>
        <inputs>
          <register_cron_add_handlers>
            <router>aiogram.Router (caller-owned, registered in tg/handlers.py build_router)</router>
            <get_user_tz>Callable[[int], Awaitable[str]] (owner_telegram_id → IANA tz)</get_user_tz>
          </register_cron_add_handlers>
        </inputs>
        <outputs>
          <register_cron_add_handlers>None — side-effect: handler registered on router</register_cron_add_handlers>
        </outputs>
        <side_effects>
          <Item>Persists 1 jobs.Job row via create_cron_user_job</Item>
          <Item>Registers 1 APScheduler CronTrigger job</Item>
          <Item>Sends 1-2 TG messages via message.answer()</Item>
        </side_effects>
        <log_anchors>
          <Item>tg.command.cron_add (entry; usage_invalid|escalate|parsed|scheduled|failed)</Item>
          <Item>tg.command.cron_add.parsed (recurrence resolved)</Item>
          <Item>tg.command.cron_add.escalate (parser returned escalate=True)</Item>
          <Item>tg.command.cron_add.scheduled (job persisted)</Item>
          <Item>tg.command.cron_add.failed (exception)</Item>
        </log_anchors>
      </M-TG-CRON-ADD>
      <M-SCHEDULER-CRON-USER>
        <file>src/ai_steward_wiki/scheduler/cron_user.py</file>
        <role>RUNTIME</role>
        <purpose>Cron-user job firing bridge — INSERT jobs.Job + register CronTrigger on AsyncIOScheduler; on fire, push CronQueueMsg to PriorityJobQueue(lane=CRON_WRITE).</purpose>
        <depends>
          <Item>apscheduler (AsyncIOScheduler, CronTrigger)</Item>
          <Item>sqlalchemy.ext.asyncio (AsyncSession, async_sessionmaker)</Item>
          <Item>structlog, pydantic</Item>
          <Item>ai_steward_wiki.storage.jobs.models.Job</Item>
          <Item>ai_steward_wiki.storage.jobs.payloads.CronUserPayload</Item>
          <Item>ai_steward_wiki.classifier.recurrence.Recurrence</Item>
          <Item>ai_steward_wiki.scheduler.queue.PriorityJobQueue, Lane</Item>
        </depends>
        <inputs>
          <set_cron_user_context>
            <scheduler>AsyncIOScheduler</scheduler>
            <queue>PriorityJobQueue</queue>
            <jobs_session_maker>async_sessionmaker[AsyncSession]</jobs_session_maker>
          </set_cron_user_context>
          <create_cron_user_job>
            <owner_telegram_id>int</owner_telegram_id>
            <chat_id>int</chat_id>
            <recurrence>Recurrence</recurrence>
            <command>str</command>
            <user_tz>str (IANA tz, e.g. 'Europe/Moscow')</user_tz>
            <wiki_id>str | None</wiki_id>
          </create_cron_user_job>
        </inputs>
        <outputs>
          <create_cron_user_job>int (jobs.id)</create_cron_user_job>
          <fire_cron_user_job>None</fire_cron_user_job>
        </outputs>
        <side_effects>
          <Item>INSERT 1 row into jobs.jobs (kind='cron_user', status='scheduled')</Item>
          <Item>scheduler.add_job(... id=str(job_id), replace_existing=True)</Item>
          <Item>On fire: queue.put(Lane.CRON_WRITE, CronQueueMsg)</Item>
        </side_effects>
        <log_anchors>
          <Item>scheduler.cron_user.scheduled (job row + cron registered)</Item>
          <Item>scheduler.cron_user.fire (callback executed; enqueued)</Item>
          <Item>scheduler.cron_user.fire.job_missing (jobs row vanished)</Item>
          <Item>scheduler.cron_user.fire.failed (push to queue raised)</Item>
        </log_anchors>
        <notes>
          <Item>Mirrors firing.py shape: module-level _ctx registry installed by set_cron_user_context; fire callback takes ONLY picklable int (SQLAlchemyJobStore-safe).</Item>
          <Item>Does NOT execute CLI — only enqueues. CLI execution is the consumer's concern.</Item>
        </notes>
      </M-SCHEDULER-CRON-USER>
      <M-SCHEDULER-CONSUMER>
        <file>src/ai_steward_wiki/scheduler/consumer.py</file>
        <role>RUNTIME</role>
        <purpose>Single async drain loop over PriorityJobQueue — for each item: spawn `systemd-run --scope --collect` wrapping claude CLI, capture stdout, timeout=600s, deliver result to chat_id via bot.send_message (chunked).</purpose>
        <depends>
          <Item>asyncio</Item>
          <Item>structlog, pydantic</Item>
          <Item>aiogram (Bot — typing-only at module level; runtime via constructor DI per R-1)</Item>
          <Item>sqlalchemy.ext.asyncio (AsyncSession, async_sessionmaker)</Item>
          <Item>ai_steward_wiki.scheduler.queue.PriorityJobQueue, QueueItem</Item>
          <Item>ai_steward_wiki.scheduler.core.kill_with_sequence</Item>
          <Item>ai_steward_wiki.claude_cli.common (resolve_binary, build_env, neutral_cwd, system_prompt_argv, truncate_stderr)</Item>
          <Item>ai_steward_wiki.tg.output.ChainSplitter (standalone reuse, ≤PART_MAX_CHARS chunking)</Item>
          <Item>ai_steward_wiki.storage.jobs.models.Job (status updates: 'running' → 'finished'|'failed')</Item>
        </depends>
        <inputs>
          <CronConsumer_constructor_ key="CronConsumer(constructor)">
            <queue>PriorityJobQueue</queue>
            <bot>aiogram.Bot</bot>
            <claude_binary>str</claude_binary>
            <claude_config_dir>pathlib.Path</claude_config_dir>
            <prompt_path>pathlib.Path</prompt_path>
            <jobs_session_maker>async_sessionmaker[AsyncSession]</jobs_session_maker>
            <timeout_s>int = 600</timeout_s>
            <slice_name>str = 'aisw-cli.slice'</slice_name>
          </CronConsumer_constructor_>
          <run>()  # blocks until cancelled</run>
        </inputs>
        <outputs>
          <run>None (raises CancelledError on shutdown)</run>
        </outputs>
        <side_effects>
          <Item>Per-item: 1 systemd-run subprocess; 1-3 bot.send_message calls (chunked output)</Item>
          <Item>Per-item: jobs.jobs row status mutated 'scheduled' → 'running' → 'finished'|'failed'</Item>
        </side_effects>
        <log_anchors>
          <Item>scheduler.consumer.drained (got queue item)</Item>
          <Item>scheduler.consumer.exec.started (subprocess spawned)</Item>
          <Item>scheduler.consumer.exec.done (exit_code=0; duration_ms)</Item>
          <Item>scheduler.consumer.exec.timeout (killed after 600s)</Item>
          <Item>scheduler.consumer.exec.failed (exit_code != 0; stderr tail)</Item>
          <Item>scheduler.consumer.delivered (bot.send_message OK; n_chunks)</Item>
          <Item>scheduler.consumer.deliver_failed (TelegramAPIError)</Item>
        </log_anchors>
      </M-SCHEDULER-CONSUMER>
      <M-STORAGE-JOBS-PAYLOAD-WIDEN>
        <file>src/ai_steward_wiki/storage/jobs/payloads.py</file>
        <role>TYPES</role>
        <purpose>Widen CronUserPayload to mirror DigestPayload shape — typed Recurrence + free-form command + optional wiki_id.</purpose>
        <change>
          <before>class CronUserPayload(_PayloadBase):
    kind: Literal[&quot;cron_user&quot;] = &quot;cron_user&quot;
    wiki_id: str
    cron_expr: str
    user_text: str
</before>
          <after>class CronUserPayload(_PayloadBase):
    kind: Literal[&quot;cron_user&quot;] = &quot;cron_user&quot;
    recurrence: Recurrence            # typed (was: cron_expr: str)
    command: str                       # was: user_text
    wiki_id: str | None = None         # was: required str
</after>
        </change>
        <migration>None — JSON column, zero existing rows, no Alembic step needed. parse_job_payload unchanged signature.</migration>
      </M-STORAGE-JOBS-PAYLOAD-WIDEN>
    </modules>
    <functional_design>
      <ux_flow_cron_add>1. User: &quot;`/cron_add каждый день в 9 утра | напомни выпить витамины`&quot;
2. Handler splits on first `|`: schedule_text=&quot;каждый день в 9 утра&quot;, command=&quot;напомни выпить витамины&quot;
3. parse_recurrence(schedule_text, user_tz=resolve_tz(owner)) → Recurrence | escalate
   3a. escalate → reply &quot;Не понял расписание. Попробуй: каждый день в 9, каждую среду в 14:00, каждого 5го в 10:00&quot; → return
   3b. invalid command (empty after pipe) → reply usage → return
4. create_cron_user_job(owner, chat, recurrence, command, user_tz, wiki_id=None) → job_id (int)
5. Reply: &quot;✅ Запланировано (id={job_id}): {recurrence_humanized}. Команда: {command_preview}&quot;
   recurrence_humanized examples (ru):
     daily      → &quot;каждый день в 09:00 (Europe/Moscow)&quot;
     weekly     → &quot;каждую среду в 14:00 (Europe/Moscow)&quot;
     monthly    → &quot;каждое 5-е число в 10:00 (Europe/Moscow)&quot;
</ux_flow_cron_add>
      <ux_flow_cron_fire>1. APScheduler fires CronTrigger at scheduled UTC moment.
2. fire_cron_user_job(job_id) callback:
   - SELECT jobs.Job WHERE id=job_id; if status != 'scheduled' or row missing → log + return (idempotent on replay)
   - Build CronQueueMsg(job_id, owner, chat, command, correlation_id=uuid4().hex, scheduled_at_utc=now())
   - await queue.put(Lane.CRON_WRITE, msg)
   - UPDATE jobs.Job SET status='queued', started_at_utc=NULL — leaving 'running' transition to consumer
3. Consumer drain loop:
   - item = await queue.get()
   - UPDATE jobs.Job SET status='running', started_at_utc=now()
   - argv = ['systemd-run', '--scope', '--collect', '--slice=aisw-cli.slice',
             '--unit=cli-{job_id}',
             '--setenv=CLAUDE_CONFIG_DIR=...',
             '--', claude_binary, *system_prompt_argv(prompt_path), command]
   - proc = await asyncio.create_subprocess_exec(*argv, stdout=PIPE, stderr=PIPE, cwd=neutral_cwd, env=build_env(...))
   - try: stdout, stderr = await asyncio.wait_for(proc.communicate(), timeout=600)
     except TimeoutError: await kill_with_sequence(proc); reply &quot;❌ Тайм-аут 10 минут&quot; → status='failed'
   - exit=0 → ChainSplitter.split(stdout.decode()) → bot.send_message * n; status='finished'
   - &quot;exit!=0 → reply \&quot;❌ Ошибка ({code}): {truncate_stderr(stderr)}\&quot;; status='failed'&quot;
</ux_flow_cron_fire>
      <module_map_visual>TG /cron_add ──→ tg/cron_add.py (M-TG-CRON-ADD)
                      │ parse_recurrence + validate
                      ▼
          scheduler/cron_user.py::create_cron_user_job
                      │ INSERT Job + scheduler.add_job
                      ▼
             [APScheduler CronTrigger fires]
                      │
                      ▼
          scheduler/cron_user.py::fire_cron_user_job (callback)
                      │ build CronQueueMsg
                      │ queue.put(Lane.CRON_WRITE, msg)
                      ▼
          scheduler/queue.py::PriorityJobQueue (asyncio.PriorityQueue)
                      │
                      ▼
          scheduler/consumer.py::CronConsumer.run (drain loop)
                      │ systemd-run --scope --collect -- claude ...
                      │ asyncio.wait_for(communicate, 600s)
                      ▼
          aiogram.Bot.send_message (chunked via ChainSplitter)
</module_map_visual>
    </functional_design>
    <queue_msg_design>
      <module>src/ai_steward_wiki/scheduler/queue_payloads.py (new, small)</module>
      <rationale>Keep scheduler/queue.py pure (Lane/QueueItem/PriorityJobQueue). Pydantic payload types in sibling module so future kinds extend the union without touching queue.py.</rationale>
      <shape>class _MsgBase(BaseModel):
    model_config = ConfigDict(extra=&quot;forbid&quot;, frozen=True)
class CronUserQueueMsg(_MsgBase):
    kind: Literal[&quot;cron_user&quot;] = &quot;cron_user&quot;
    job_id: int
    owner_telegram_id: int
    chat_id: int
    command: str
    correlation_id: str
    scheduled_at_utc: datetime  # UTC
QueueMsg = Annotated[CronUserQueueMsg, Field(discriminator=&quot;kind&quot;)]
# discriminator left in place so future kinds add a sibling without
# widening callers — NFR-5.
</shape>
    </queue_msg_design>
    <approach_decisions>
      <Item id="AD-01">
        <title>Producer/consumer split (not direct fire)</title>
        <options>
          <A>Producer enqueues + separate consumer dequeues+executes [chosen]</A>
          <B>APScheduler callback directly spawns CLI + delivers (skip queue) [rejected]</B>
          <C>Two-tier consumer with separate exec + deliver queues [rejected]</C>
        </options>
        <decision>A</decision>
        <rationale>Q&amp;A 2026-05-14 chose Variant1 (walking skeleton with real queue). B defeats aisw-02v's purpose (queue must be load-bearing). C is premature abstraction — single failure mode + single consumer is simpler and meets NFR-1 (concurrency=1).</rationale>
      </Item>
      <Item id="AD-02">
        <title>No interactive confirm in walking skeleton</title>
        <options>
          <A>Auto-schedule on parse-success; reply renders parsed recurrence as proof [chosen]</A>
          <B>Explicit ConfirmationService (D-023) inline keyboard confirm/cancel [rejected]</B>
          <C>1-button cancel keyboard with cron_cancel:&lt;id&gt; callback [rejected]</C>
        </options>
        <decision>A</decision>
        <rationale>Discovery scope.out excludes /cron_delete and /cron_edit. Adding confirm without delete creates UX asymmetry. Parsed-cron echo in reply (humanized ru) gives sufficient visual verification — parser is rule-based (deterministic) per M-CLASSIFIER-RECURRENCE. If parser returns escalate=True, handler rejects with usage hint (no silent failure). /cron_confirm + /cron_delete + /cron_edit are a single follow-up bd issue.</rationale>
      </Item>
      <Item id="AD-03">
        <title>Reuse ChainSplitter standalone vs. full deliver_output(kind='reply')</title>
        <options>
          <A>ChainSplitter + bot.send_message [chosen]</A>
          <B>Full deliver_output (persistence + audit run_outputs) [rejected]</B>
        </options>
        <decision>A</decision>
        <rationale>Full D-025 persistence is concerned with run-output indexing for digest re-render. Cron-user runs are user-initiated and ephemeral — no need for /expand or document fallback in walking skeleton. ChainSplitter call signature is stable (ChainSplitter().split(text) returns list[str]); swap to deliver_output is a clean refactor in a future iteration when run-history matters.</rationale>
      </Item>
      <Item id="AD-04">
        <title>Lane.CRON_WRITE (=2), not 'USER' as in discovery</title>
        <options>
          <A>Lane.CRON_WRITE [chosen]</A>
          <B>Lane.USER_WRITE [rejected — wrong semantics]</B>
        </options>
        <decision>A</decision>
        <rationale>queue.py enum already has CRON_WRITE=2 between USER_WRITE=1 (interactive user write) and DIGEST=3. Cron-fired jobs are scheduled writes, not interactive — discovery's 'USER' was a paraphrase, not the canonical enum name. CRON_WRITE = 2 keeps USER_WRITE = 1 reserved for synchronous user-driven WIKI writes (the inbox path).</rationale>
      </Item>
      <Item id="AD-05">
        <title>Widen CronUserPayload (typed Recurrence) — no Alembic migration</title>
        <options>
          <A>Widen in-place [chosen]</A>
          <B>New CronUserV2Payload, deprecate old [rejected]</B>
        </options>
        <decision>A</decision>
        <rationale>CronUserPayload is currently never persisted (no row exists with kind='cron_user'). Pydantic discriminated union is read at parse time only; the column is JSON. Zero rows, zero serialized references — replacing the model in place is safe. Test suite must grep for any test using .cron_expr / .user_text and update.</rationale>
      </Item>
      <Item id="AD-06">
        <title>Consumer ownership of jobs.Job status transitions</title>
        <options>
          <A>Producer sets 'queued' on enqueue; consumer flips 'running' → 'finished'|'failed' [chosen]</A>
          <B>Producer sets 'running'; consumer flips terminal [rejected]</B>
        </options>
        <decision>A</decision>
        <rationale>Producer enqueues to in-memory queue (microsecond op); 'running' should reflect actual subprocess spawn. Three terminal states give better observability for follow-up cron_list. Status enum extension: 'scheduled' (initial after cron_add) → 'queued' (producer enqueued) → 'running' (consumer spawned process) → 'finished' | 'failed' | 'cancelled' (future).</rationale>
      </Item>
      <Item id="AD-07">
        <title>Single consumer task in bot lifecycle, NOT separate process</title>
        <options>
          <A>asyncio.create_task in __main__ startup, cancel on shutdown [chosen]</A>
          <B>Separate systemd service [rejected]</B>
        </options>
        <decision>A</decision>
        <rationale>Bot already owns aiogram.Bot ref required for delivery. Single-process keeps in-memory queue intact (vs requiring IPC). NFR-1 specifies single-drain at-most-once. Acceptable for walking skeleton; multi-worker is a follow-up if backpressure becomes real.</rationale>
      </Item>
    </approach_decisions>
    <verification_plan_sketch>
      <unit_tests>
        <Item>tests/unit/test_cron_add_handler.py — parse path (happy / escalate / no pipe / empty command)</Item>
        <Item>tests/unit/test_cron_user_producer.py — create_cron_user_job inserts row + scheduler.add_job called with correct CronTrigger; fire_cron_user_job pushes to mock queue</Item>
        <Item>tests/unit/test_cron_consumer.py — fake queue + fake bot + stub spawn (Spawner Protocol): happy path (exit=0 → send_message), timeout path (kill_with_sequence called), non-zero exit (error message sent)</Item>
        <Item>tests/unit/test_queue_payloads.py — Pydantic discriminator round-trip</Item>
        <Item>tests/unit/test_payloads_cron_user_widened.py — Recurrence field, command field, wiki_id optional</Item>
      </unit_tests>
      <integration_test>
        <Item>tests/integration/test_cron_add_flow.py — register handler on real Router, simulate /cron_add message, advance APScheduler clock manually, assert queue gets item, run 1 consumer iteration with stub Spawner returning canned stdout, assert bot.send_message(chat_id, expected_text) was called</Item>
      </integration_test>
      <coverage_target>≥80% on the 3 new modules (per NFR-6)</coverage_target>
    </verification_plan_sketch>
  </Feature>
  <Feature source="20260620-hang-diagnostics-design.md" bd_id="aisw-xbc">
    <feature>hang-diagnostics</feature>
    <module_id>M-FOUNDATION-LOGGING</module_id>
    <status>stable</status>
    <date>2026-06-20</date>
    <stack>
      <Item>
        <library>structlog</library>
        <version>24.4.0 (uv.lock)</version>
        <used_for>JSON-lines events for heartbeat/lag/anchors/lifecycle; reuse traced + logging_events SSoT</used_for>
      </Item>
      <Item>
        <library>aiogram</library>
        <version>3.15.0 (uv.lock)</version>
        <used_for>AiogramSender boundary anchors; CorrelationMiddleware lifecycle event</used_for>
      </Item>
      <Item>
        <library>asyncio (stdlib)</library>
        <version>py3.11+</version>
        <used_for>heartbeat task, loop.add_signal_handler(SIGUSR1), asyncio.all_tasks()+Task.get_stack() dump</used_for>
      </Item>
      <Item>
        <library>faulthandler (stdlib)</library>
        <version>py3.11+</version>
        <used_for>C-level thread-stack dump on SIGUSR1 (works even if loop callbacks are wedged)</used_for>
      </Item>
    </stack>
    <decisions>
      <Item>
        <D-local-1>New module src/ai_steward_wiki/ops/observability.py owns ALL diagnostics — heartbeat+lag loop, SIGUSR1 install, asyncio task-stack dump, faulthandler register. Single SoC home, testable in isolation.</D-local-1>
      </Item>
      <Item>
        <D-local-2>Heartbeat measures lag via monotonic drift — loop expected to wake every interval; lag_ms = (actual_monotonic - expected_monotonic)*1000. Emits runtime.loop.heartbeat each tick (INFO, cheap) and runtime.loop.lag (WARNING) only when lag_ms &gt; obs_loop_lag_warn_ms.</D-local-2>
      </Item>
      <Item>
        <D-local-3>Auto-dump — when lag_ms &gt; obs_loop_lag_dump_ms (a higher bar), trigger the same dump as SIGUSR1, rate-limited by obs_dump_min_interval_s.</D-local-3>
      </Item>
      <Item>
        <D-local-4>Boundary anchors reuse the existing traced decorator (logging_setup.py:104) but with a threshold wrapper — log .done only when duration_ms &gt; obs_io_slow_threshold_ms, ALWAYS log .error. Avoids happy-path noise (hybrid cost). Applied to AigramSender methods + the audit write in deliver_output.</D-local-4>
      </Item>
      <Item>
        <D-local-5>Handler lifecycle — CorrelationMiddleware.__call__ wraps handler in try/finally measuring duration; emits tg.update.handled (duration_ms, failed:bool) always (one line per update is acceptable, low traffic), and a tg.update.handler_slow WARNING when over obs_handler_slow_threshold_ms.</D-local-5>
      </Item>
      <Item>
        <D-local-6>Stack dump format — asyncio tasks dumped as structlog event runtime.diag.task_dump with a list of {name, frames:[file:line:func + source-line]} (NO arg values). faulthandler.dump_traceback(all_threads=True) writes plain text to stderr→journald. Both fire on SIGUSR1; faulthandler is the C-level fallback that survives a wedged loop.</D-local-6>
      </Item>
      <Item>
        <D-local-7>Lifecycle wiring in __main__ — create heartbeat task next to polling_task/consumer_task; install SIGUSR1 via loop.add_signal_handler; faulthandler.enable() at startup; cancel heartbeat + remove signal handler in the RUNTIME_SHUTDOWN finally block.</D-local-7>
      </Item>
      <Item>
        <D-local-8>Config via Settings (AISW_ prefix), mirroring storage_slow_query_threshold_ms precedent — obs_heartbeat_interval_s=20.0, obs_loop_lag_warn_ms=500, obs_loop_lag_dump_ms=5000, obs_io_slow_threshold_ms=1000, obs_handler_slow_threshold_ms=5000, obs_dump_min_interval_s=60.0. No magic numbers.</D-local-8>
      </Item>
      <Item>
        <D-local-9>New event-name constants centralised in logging_events.py (SSoT): RUNTIME_LOOP_HEARTBEAT, RUNTIME_LOOP_LAG, RUNTIME_DIAG_TASK_DUMP, TG_UPDATE_HANDLED, TG_UPDATE_HANDLER_SLOW, plus IO anchor prefixes.</D-local-9>
      </Item>
    </decisions>
  </Feature>
  <Feature source="20260623-compact-reply-design.md" bd_id="aisw-2n2">
    <feature>compact-reply</feature>
    <status>approved</status>
    <risk>medium</risk>
    <approach>final-turn-extractor + 3 prose call-sites rewired + wiki.md format rules + loader progress</approach>
    <adr_candidate>False</adr_candidate>
    <libraries_touched/>
  </Feature>
  <Feature source="20260623-html-escape-outbound-design.md" bd_id="aisw-azu">
    <feature>html-escape-outbound</feature>
    <module_id>M-TG-OUTPUT</module_id>
    <status>stable</status>
    <date>2026-06-23</date>
    <stack>
      <Item>
        <library>html (stdlib)</library>
        <version>py3.11+</version>
        <used_for>entity escaping of non-whitelist text (&amp;, &lt;, &gt;)</used_for>
      </Item>
      <Item>
        <library>re (stdlib)</library>
        <version>py3.11+</version>
        <used_for>tokenize text by ALLOWED_TAGS regex; entity-aware &amp; escaping in attrs</used_for>
      </Item>
      <Item>
        <library>aiogram</library>
        <version>3.15.0 (uv.lock)</version>
        <used_for>TelegramBadRequest detection for the parse_mode=None fallback</used_for>
      </Item>
    </stack>
    <decisions>
      <Item>
        <D-local-1>Two-layer defence. Layer 1 (primary, root cause) — sanitize_html() escapes all markup EXCEPT whitelisted tags before send. Layer 2 (safety net) — AiogramSender.send_message retries once with parse_mode=None if Telegram still rejects with &quot;can't parse entities&quot;.</D-local-1>
      </Item>
      <Item>
        <D-local-2>sanitize_html tokenizes by an ALLOWED_TAGS-only regex (built from the existing ALLOWED_TAGS set — no fork). Gaps between matched tags + any non-whitelisted &quot;&lt;...&gt;&quot;-like text are escaped via _escape_text (&amp; → &amp;amp;, then &lt; → &amp;lt;, then &gt; → &amp;gt;, order matters). Whitelisted tags are emitted verbatim except bare &quot;&amp;&quot; in their attrs is escaped with a negative-lookahead (skip existing &amp;amp;/&amp;lt;/&amp;gt;/&amp;quot;/&amp;#nn;).</D-local-2>
      </Item>
      <Item>
        <D-local-3>Apply once at the TOP of deliver_output (text = sanitize_html(text)) so inline / chain-split / summary paths all operate on already-valid HTML. HtmlBalancer then sees only whitelisted tags (its existing contract holds).</D-local-3>
      </Item>
      <Item>
        <D-local-4>Fallback scope — only on aiogram.exceptions.TelegramBadRequest whose message contains &quot;can't parse entities&quot;; resend with parse_mode=None (plain text, markup shown literally) and emit tg.io.send_message.parse_fallback WARNING. Any other TelegramBadRequest re-raises unchanged. Retry happens because the first call raised before returning — nothing was delivered, so no double-send.</D-local-4>
      </Item>
      <Item>
        <D-local-5>sanitize_html lives in output.py next to ALLOWED_TAGS/_TAG_RE (SSoT for the whitelist) and is exported for reuse/testing.</D-local-5>
      </Item>
    </decisions>
  </Feature>
  <Feature source="20260623-md-to-tg-html-design.md" bd_id="aisw-iyz">
    <feature>md-to-tg-html</feature>
    <module_id>M-TG-OUTPUT</module_id>
    <status>stable</status>
    <date>2026-06-23</date>
    <stack>
      <Item>
        <library>markdown-it-py</library>
        <version>4.2.0 (already in uv.lock; promote to direct dependency, pinned ==)</version>
        <used_for>CommonMark tokenization (MarkdownIt().parse → token stream); custom token-walk renderer to TG-HTML</used_for>
      </Item>
      <Item>
        <library>re (stdlib)</library>
        <version>py3.11+</version>
        <used_for>pre-pass pipe-table flattener; bare-&amp; handling already in output.sanitize_html</used_for>
      </Item>
    </stack>
    <decisions>
      <Item>
        <D-local-1>New module tg/md_to_html.py owns markdown_to_tg_html(text)-&gt;str. SRP — input GitHub-Markdown, output TG-HTML using only ALLOWED_TAGS. Imports ALLOWED_TAGS from output.py (no whitelist fork).</D-local-1>
      </Item>
      <Item>
        <D-local-2>Parse with MarkdownIt() (CommonMark). Walk tokens (verified types — heading_open/close, paragraph_open/close, inline+children {text, strong_open/close, em_open/close, s_open/close, code_inline, link_open/close}, bullet_list/ordered_list + list_item, fence, blockquote_open/close). Emit a string.</D-local-2>
      </Item>
      <Item>
        <D-local-3>Token → TG-HTML map. strong→&lt;b&gt;, em→&lt;i&gt;, s→&lt;s&gt;, code_inline→&lt;code&gt;, fence→&lt;pre&gt;, link_open(href)→&lt;a href=&quot;...&quot;&gt;, blockquote→&lt;blockquote&gt;. Text nodes HTML-escaped (reuse output._escape_text). heading_open→ emit &lt;b&gt;, heading_close→&lt;/b&gt;+newline (DEGRADE: no TG heading). bullet list_item→ prefix &quot;• &quot;; ordered list_item→ &quot;N. &quot; (counter). paragraph_close→ &quot;\n\n&quot; block separator (trimmed at end).</D-local-3>
      </Item>
      <Item>
        <D-local-4>Enable strikethrough rule if available (md.enable(&quot;strikethrough&quot;, ignoreInvalid=True)); s_open/close → &lt;s&gt;. If unavailable, &quot;~~&quot; stays as escaped text (acceptable degradation).</D-local-4>
      </Item>
      <Item>
        <D-local-5>Tables — markdown-it core has no table rule. A pre-pass flattens GFM pipe-table rows (lines matching ^\\s*\\|.*\\|\\s*$ and the |---|--- separator) into plain &quot; • cell — cell&quot; lines BEFORE MarkdownIt, dropping the separator row. No mdit-py-plugins dependency.</D-local-5>
      </Item>
      <Item>
        <D-local-6>Link href safety — escape the URL for HTML attribute (bare &amp; → &amp;amp;) reusing the sanitize_html convention; skip non-http(s) schemes (emit link text only) to avoid javascript: etc.</D-local-6>
      </Item>
      <Item>
        <D-local-7>Pipeline order in deliver_output — text = markdown_to_tg_html(text) FIRST (produces valid TG-HTML), THEN text = sanitize_html(text) as the idempotent safety net (no double-escape, since converter already escaped text and emitted whitelist tags). Persist+send the converted text.</D-local-7>
      </Item>
      <Item>
        <D-local-8>Safety net unchanged — the aisw-azu parse_mode=None fallback still catches any residual invalid HTML, so a reply is never lost even if the converter has a bug.</D-local-8>
      </Item>
    </decisions>
  </Feature>
  <Feature source="20260626-classifier-quality-design.md">
    <feature>classifier-quality</feature>
    <date>20260626</date>
    <beads>
      <Item>aisw-32p</Item>
      <Item>aisw-zgf</Item>
    </beads>
    <status>design</status>
  </Feature>
  <Feature source="20260626-query-gap-design.md" bd_id="aisw-50z">
    <feature>query-gap</feature>
    <module_id>M-TG-PIPELINE</module_id>
    <status>stable</status>
    <date>2026-06-26</date>
    <risk>medium</risk>
    <evidence>strong</evidence>
    <open_questions/>
    <stack>
      <Item>
        <library>structlog</library>
        <version>pinned (uv.lock)</version>
        <used_for>existing tg.pipeline.runner.dispatched / deliver.sent anchors (no new anchor needed)</used_for>
      </Item>
      <Item>
        <library>pytest / pytest-asyncio</library>
        <version>pinned (uv.lock)</version>
        <used_for>RED-&gt;GREEN unit test on the routing predicate</used_for>
      </Item>
    </stack>
    <decisions>
      <Item>
        <D-local-1>Remove Intent.WIKI_QUERY from _ROUTABLE_INTENTS (pipeline.py:585). This is the whole behavioural fix. _ROUTABLE_INTENTS becomes {WIKI_INGEST, UNKNOWN}. wiki_query then skips HINT_FASTPATH (1157) and ROUTABLE_BRANCH (1278) — both gated on the set — and, being neither REMINDER/DIGEST/ADMIN, falls through to the generic answer runner (1404+).</D-local-1>
      </Item>
      <Item>
        <D-local-2>Theme-picking is DELEGATED to the Claude run, not done in the pipeline. The generic runner runs in the user ROOT (wiki_root/&lt;telegram_id&gt;) with --add-dir on root, so the run reads across all &lt;Domain&gt;-WIKI/ and answers from the relevant one. This is the e2e spec's &quot;read across the user's WIKIs&quot; branch and is always correct. score_catalog/is_confident stay reserved for the INGEST fast-path only.</D-local-2>
      </Item>
      <Item>
        <D-local-3>NO cwd-scoping to a single matched theme WIKI in this fix. Scoping would require threading a target wiki_path through the WikiRunner Protocol + _WikiRunnerAdapter + StreamingDelivery + pipeline (&gt;=4 modules =&gt; HIGH risk, breaks the medium-risk envelope). Deferred as a LATER optimization (focus/latency), not a correctness requirement.</D-local-3>
      </Item>
      <Item>
        <D-local-4>NO new user-facing copy and NO new log anchor. The generic runner already logs tg.pipeline.runner.dispatched(intent=wiki_query) + tg.pipeline.deliver.sent and delivers assistant text (ACK_TEXT_RU only as empty-output fallback). The e2e log_watch scorer reads exactly this trace.</D-local-4>
      </Item>
      <Item>
        <D-local-5>wiki_ingest and unknown are UNCHANGED — they stay in _ROUTABLE_INTENTS and keep router / hint-fast-path filing.</D-local-5>
      </Item>
    </decisions>
  </Feature>
  <Feature source="20260702-adaptive-scope-design.md" bd_id="aisw-o6m">
    <feature>adaptive-scope</feature>
    <module_id>M-WIKI-SCOPE</module_id>
    <status>stable</status>
    <date>2026-07-02</date>
    <risk>medium</risk>
    <evidence>strong</evidence>
    <open_questions/>
    <stack>
      <Item>Python 3.11 stdlib only for the new module (re, dataclasses, pathlib) — mirrors inbox/hint_match.py which is deliberately stdlib-only.</Item>
      <Item>Reused in-project machinery — inbox/hint_match (score_catalog, is_confident, MIN_SCORE, MIN_MARGIN, MAX_FASTPATH_CHARS), wiki/migration (MANAGED_START/MANAGED_END markers), __main__.py factories (make_hint_catalog_resolver, _resolve_owner_wikis_factory), wiki/runner assemble_prompt CLAUDE.md append.</Item>
      <Item>structlog for the wiki.run.scope anchor (existing logging_setup pipeline).</Item>
      <Item>No new external dependencies; no Context7 triggers (stdlib + in-repo APIs only).</Item>
    </stack>
    <decisions>
      <Item>DEC-1 Scope resolution lives in the runner adapter (__main__.py), NOT in tg/pipeline.py — the adapter already receives text, intent, owner_telegram_id; WikiRunner Protocol and dispatch stay untouched (kills the aisw-50z Protocol-change blocker). ADR-034.</Item>
      <Item>DEC-2 New module src/ai_steward_wiki/wiki/scope.py (M-WIKI-SCOPE) holds the pure logic — ScopeDecision frozen dataclass, resolve_query_scope(text, catalog), collect_layouts(wikis) — so __main__.py keeps only thin wiring and the logic is unit-testable without adapters.</Item>
      <Item>DEC-3 Scoped run = wiki_path swap only. When confident, the adapter passes the matched WIKI dir as wiki_path to run_wiki_session — cwd, --add-dir and CLAUDE.md-append all follow automatically (runner.py:552-556, 271-274). No changes inside wiki/runner.py.</Item>
      <Item>DEC-4 Cross run injection point = the existing per-run scratch overlay (__main__.py:438-440). Overlay becomes semver line + &quot;# User turn&quot; + &quot;# Карта WIKI пользователя&quot; block with managed-zone excerpts of every domain WIKI.</Item>
      <Item>DEC-5 Digest injection point = planner_context (adapter-level, _DigestRunnerAdapter.__call__), appending layouts of non-primary WIKIs only — primary CLAUDE.md already arrives via assemble_prompt.</Item>
      <Item>DEC-6 Same confidence gates as prod ingest fast-path — score_catalog + is_confident + len(text) &lt;= MAX_FASTPATH_CHARS. No new thresholds, no settings knobs (hint_match.py YAGNI precedent).</Item>
      <Item>DEC-7 Graceful degradation — any resolver/IO error in scope resolution logs and falls back to today's cross-root behaviour; a scope decision can never fail a run.</Item>
    </decisions>
  </Feature>
  <Feature source="20260702-nutrition-tracking-design.md" bd_id="aisw-2si">
    <feature>nutrition-tracking</feature>
    <module_id>M-WIKI-LIFECYCLE</module_id>
    <status>stable</status>
    <date>2026-07-02</date>
    <risk>low</risk>
    <evidence>strong</evidence>
    <open_questions/>
    <stack>
      <Item>Markdown domain templates (templates/medical.md, templates/cooking.md) — managed-zone source, propagated via wiki/migration.py repair_managed_zone.</Item>
      <Item>Markdown Stage-1b overlay prompt (prompts/domain-medical.md) — read per CLI run, semver-versioned.</Item>
      <Item>CSV data file convention (diet/food_log.csv) — same accumulating-file pattern as metrics/weight.csv.</Item>
      <Item>scripts/backfill_managed_zone.py — existing idempotent prod propagation tool (no new code).</Item>
    </stack>
    <decisions>
      <Item>DEC-1 Nutrition lives INSIDE Medical-WIKI (diet/), not a separate Nutrition domain — a third food domain (after Cooking) would violate SSoT and add a new classifier fork; chosen as Variant 3 in /best-approach (88%), user confirmed. ADR-033.</Item>
      <Item>DEC-2 food_log.csv schema `date,meal,item,qty_g,kcal,source` with source=logged|estimated — industry minimum; the source flag separates user-stated values from model estimates (~38% avg error in crowdsourced calorie data) and legally/ethically marks estimates as non-clinical.</Item>
      <Item>DEC-3 Calorie estimation of self-logged food is classified as data structuring, NOT medical interpretation — allowed without consent; clinical nutrition topics stay under the doctor redirect.</Item>
      <Item>DEC-4 No consent for food_log appends — explicit confirm remains deletion-only (D-041 pre-flight unchanged); consent dialogs are structurally broken over stateless per-message CLI runs.</Item>
      <Item>DEC-5 Routing determinism via Inbox hint keywords (Medical gains еда/калории/ккал/съел/порция) + reciprocal demarcation lines in both medical.md and cooking.md.</Item>
      <Item>DEC-6 No macros, no OCR, delete NUTRITION_GUIDE.md — user decisions 2026-07-02 (YAGNI).</Item>
    </decisions>
  </Feature>
  <Feature source="20260703-codex-subscription-fallback-design.md" bd_id="aisw-8gw">
    <feature>codex-subscription-fallback</feature>
    <module_id>M-LLM-FAILOVER</module_id>
    <status>stable</status>
    <date>2026-07-03</date>
    <risk>high</risk>
    <evidence>strong</evidence>
    <open_questions/>
    <adr>ADR-035</adr>
    <stack>
      <Item>Python 3.11 stdlib asyncio, dataclasses, enum, pathlib, and typing for provider state, concurrency, and typed results.</Item>
      <Item>Existing Pydantic v2 schemas remain the validation boundary for Stage-0 structured output.</Item>
      <Item>Existing structlog JSON logging remains the observability boundary.</Item>
      <Item>Claude Code CLI remains the primary subscription provider through the existing saved authentication.</Item>
      <Item>Codex CLI 0.142.5 is the pinned fallback runtime, authenticated through ChatGPT subscription login in a dedicated CODEX_HOME.</Item>
      <Item>gpt-5.4-mini with low reasoning serves Stage-0 classification and lightweight structured parsers.</Item>
      <Item>gpt-5.5 with medium reasoning serves WIKI, router, digest, web, schema-generation, and cron fallback flows.</Item>
      <Item>codex exec uses JSON Schema output for structured mode and JSONL output for agent mode.</Item>
      <Item>No new Python dependency and no database migration are required by the selected design.</Item>
    </stack>
    <decisions>
      <Item>DEC-1 Use a shared provider-neutral failover policy with separate Claude and Codex adapters. Do not duplicate fallback logic in each call site and do not add a sidecar service.</Item>
      <Item>DEC-2 Keep mode-specific adapters for structured, agent, and text execution. Each adapter preserves the existing consumer contract.</Item>
      <Item>DEC-3 The circuit states are named claude, codex, and probe. Conventional closed/open/half-open names are intentionally not used.</Item>
      <Item>DEC-4 Only a typed Claude subscription-limit error may move the circuit from claude to codex. Timeout, malformed output, permission denial, and generic process failures keep their current handling.</Item>
      <Item>DEC-5 Parse the provider reset time when available. Otherwise use a 15-minute codex interval followed by one single-flight probe.</Item>
      <Item>DEC-6 A successful probe moves the circuit to claude. Another subscription limit moves it back to codex.</Item>
      <Item>DEC-7 Hold the existing WIKI lock across the Claude attempt and any safe Codex fallback attempt.</Item>
      <Item>DEC-8 Replay is allowed only before mutation evidence. Write, Edit, file-change events, mutation-capable shell commands, and unknown actions block replay.</Item>
      <Item>DEC-9 Structured mode maps Claude Haiku work to gpt-5.4-mini with low reasoning and validates the existing Pydantic result schema.</Item>
      <Item>DEC-10 Agent and text modes map Claude Sonnet work to gpt-5.5 with medium reasoning.</Item>
      <Item>DEC-11 Codex write flows use workspace-write restricted to the selected WIKI. Read-only, web, structured, and cron flows use narrower capability profiles.</Item>
      <Item>DEC-12 Web fallback uses a neutral empty working directory, read-only sandbox, enabled web search, and no WIKI directory access.</Item>
      <Item>DEC-13 Production Codex authentication uses ChatGPT subscription login in a dedicated CODEX_HOME under the existing service user. API-key billing is excluded.</Item>
      <Item>DEC-14 Every Codex invocation uses explicit model, reasoning, sandbox, working directory, ignore-user-config, and ephemeral settings.</Item>
      <Item>DEC-15 Codex JSONL is normalized into provider-neutral events before existing WIKI aggregation, transcript, and delivery logic consumes it.</Item>
      <Item>DEC-16 If both providers are unavailable, preserve the source operation and return one Russian diagnostic. Do not schedule automatic replay.</Item>
      <Item>DEC-17 Provider state is process-local for this feature. Durable state across restarts remains later scope.</Item>
      <Item>DEC-18 Startup preflight checks binary, pinned version, login status, and CODEX_HOME readability without invoking a model.</Item>
      <Item>DEC-19 Deployment smoke tests verify both models, structured output, JSONL, read-only isolation, and workspace-write containment.</Item>
      <Item>DEC-20 Emit provider selection, failover, circuit transition, failure, recovery, and replay-blocked events without prompts or credentials.</Item>
    </decisions>
  </Feature>
</Technology>
