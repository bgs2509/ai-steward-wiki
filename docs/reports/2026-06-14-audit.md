# Full audit — ai-steward-wiki — 2026-06-14

> Scope: ADR-009 refactor (commit `1e2debd`, branch `worktree-adr-dev-life-separation`).
> ⚠ **PARTIAL RUN** — change-scoped audit. The full repo-wide deterministic pipeline
> (integration tests, sentrux MCP suite, uvx radon/vulture/interrogate, pip-audit,
> `make total-test` re-run with real Claude CLI) was **not** executed here: this is an
> ephemeral git worktree without Claude CLI subscription auth, so the integration /
> CLI-subprocess steps cannot pass for environmental (not code) reasons. Run the full
> `/full-audit --ai` on the main checkout / CI for repo-wide coverage.

- HEAD: `1e2debd`
- working tree: clean (committed)

## Summary

| # | Check | Status | Metric |
|---|-------|--------|--------|
| 1 | ruff check | ✓ PASS | 0 issues (via make total-test) |
| 2 | ruff format --check | ✓ PASS | 254 files, 0 reformat |
| 3 | mypy --strict src | ✓ PASS | 0 errors |
| 8 | grace lint --failOn errors | ✓ PASS | governed=91 xml=3 errors=0 warnings=0 |
| - | inv-lint | ✓ PASS | 14/14 |
| 6 | pytest tests/unit + coverage | ✓ PASS | 864 passed, 0 failed, coverage 86.82% |
| 16 | vulture (changed files) | ✓ PASS | 0 dead-code from slot/guard/property removal |
| 7 | pytest tests/integration | ⚠ NOT RUN | needs Claude CLI auth (env) |
| 20a-e | sentrux MCP suite | ⚠ NOT RUN | repo-wide, run on main checkout |
| 14-18 | radon/interrogate/pip-audit | ⚠ NOT RUN | repo-wide, run on main checkout |

Total (change-scoped): 7 PASS / 0 FAIL. Deterministic quality gate (`make total-test`) green.

## Details

### make total-test — PASS
All six steps PASS: ruff-check, ruff-format-check, mypy, grace-lint, inv-lint,
test-cov (864 passed, coverage 86.82%, threshold 80%). `MAKE_EXIT=0`.

### vulture (changed source) — PASS
`uvx vulture src/ai_steward_wiki/settings.py src/ai_steward_wiki/__main__.py
--min-confidence 80` → exit 0. Removing the two slots, the `@property`, and the three
`is None` guards left no orphaned imports/vars.

## Trends
Not computed (change-scoped partial run; `--no-trends` semantics). Pre-existing backlog
items unrelated to this change remain tracked: `aisw-358` (pypdf CVE), `aisw-rui`
(17 specs frontmatter).

## AI deep code analysis

Full pass through the 7 categories, scoped to the ADR-009 diff (`settings.py`,
`__main__.py`, `.env.example`, `knowledge-graph.xml`, tests).

### Critical
_None._

### Middle
_None._

### Minor

1. `docs/superpowers/specs/20260510-foundation-scaffold-design.md:61-62` and
   `docs/superpowers/specs/20260510-classifier-stage0-design.md:174-180` — these
   historical design snapshots still show the old two-slot
   `claude_config_dir_local/_vps` + env-resolving property, now superseded by ADR-009.
   They are dated design records (pre-decision), not current SSoT, so left as history;
   filed as a follow-up to optionally add a "superseded by ADR-009" note.
   why: a future reader grepping the specs could mistake the old shape for current.
   fix: add a one-line superseded banner, or leave as dated history (filed: follow-up).

2. `tests/unit/test_settings_config_dir.py` — the `AISW_CLAUDE_CONFIG_DIR` env-var →
   field mapping is exercised only via the direct `claude_config_dir=` kwarg, not via a
   real environment variable read.
   why: the env_prefix wiring is standard pydantic-settings behavior, but an
   env-driven test would close the loop end-to-end.
   fix: add a monkeypatch.setenv("AISW_CLAUDE_CONFIG_DIR", ...) case (filed: follow-up).

### Open questions
_None._

_Modules inspected: src/ai_steward_wiki/settings.py, src/ai_steward_wiki/__main__.py,
tests/unit/test_settings_config_dir.py, tests/unit/classifier/test_settings_inv6.py,
.env.example, docs/knowledge-graph.xml._
