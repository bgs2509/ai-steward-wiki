# SSoT: tech-spec §10.1 "Per-user UID" + D-038.
# Drop-in for systemd-sysusers. Idempotent — applied via `systemd-sysusers`
# at install time AND by the bot at first WIKI provisioning for a new user
# (creates aisw-<N> on demand and persists into users.toml).
#
# Type Name           ID    GECOS                                  Home dir                                   Shell
g     aisw-claude     -     "ai-steward-wiki shared CLAUDE_CONFIG_DIR readers"   -                                          -
u     aisw-bot        -     "ai-steward-wiki bot (orchestrator)"   /opt/ai-steward-wiki                       /usr/sbin/nologin
m     aisw-bot        aisw-claude
